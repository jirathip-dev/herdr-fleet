# .report-105.md — one bounded REAL native run of the public CLI + daemon + adapter path

Issue #105 (bounded native-execution acceptance for "the public product path
actually does something useful on a real host"). Branch `issue-105-native-proof`,
base `origin/staging` @ `7b876ab09053816e723f3646c479246b4dc1a9e2` — the
candidate head every claim below was measured at (gates and the native run
ran against exactly that tracked tree; this report file is the only later
commit, and the delivered head sha is reported in the lane message). This run
is a bounded demonstration by the merged code, **not** a full-replacement,
autonomy, or production-readiness claim: exactly ONE daemon-applied plan step
was executed, on disposable local state, and every remaining manual/orchestrator
step is enumerated in section 6. Commit wording `Refs #105`; no PR, issue, or
remote state was mutated by this lane.

The run happened in the issue-bound worktree at the pinned staging head with an
ISOLATED XDG state/config (`run/state`, `run/config`) and an isolated
`CARGO_TARGET_DIR=target-105`; all logs live under `run/logs/` (named files,
raw exit codes; lane-local and git-excluded, never part of the public tree).
`HOME` was deliberately left real so authenticated `gh` could be exercised.
The real public repository `jirathip-dev/canter` was used for the READ surface
(config/plan/status); the daemon MUTATION targeted a disposable local clone of
the pinned head (`run/repo105`) plus a lane worktree root (`run/worktrees105`).
Nothing was pushed to any remote; no GitHub mutation was performed.

## 1. Read-only surface (raw exits; logs under `run/logs/`)

| command | RAW exit | log | observed |
| --- | --- | --- | --- |
| `herdr-fleet config init` | 0 | `run/logs/01-config-init.out` | annotated `hf-config/v1` template printed; the CLI writes nothing (operator writes `run/config/herdr-fleet/config.toml` naming `jirathip-dev/canter`, branch `staging`, `[daemon] enabled` + socket) |
| `herdr-fleet config validate --json` | 0 | `run/logs/02-config-validate.json` | `valid:true`, `policy_overlay:null` |
| `herdr-fleet doctor --json` (isolated XDG config home) | 3 | `run/logs/03-doctor.json` | git ok, herdr 0.9.0 ok, **gh degraded**: the isolated `XDG_CONFIG_HOME` hides gh's host file, so the probe sees "not logged into any GitHub hosts" |
| `herdr-fleet doctor --json` (real XDG config home) | 0 | `run/logs/03b-doctor-realconfig.json` | git ok, herdr 0.9.0 ok, gh 2.86.0 authenticated (scopes reported); config row `absent` (the isolated config is only reachable via `--config`) |
| `herdr-fleet status --json` (isolated XDG config home) | 3 | `run/logs/04-status.json` | degradations reported, never hidden: `herdr` complete; repository observation `completeness:partial` with `github.available:false` (same isolated-gh cause) and `git.origin_matches:false` |
| `herdr-fleet status --json --config run/config/herdr-fleet/config.toml` (real XDG config home) | 0 | `run/logs/04b-status-realconfig.json` | `observed_repositories:1`, `partial_failures:0`: herdr ok; git `branch=issue-105-native-proof`, `head=7b876ab…` (pinned), `origin_matches:false`; github `available:true`, `remote_identity:jirathip-dev/canter`, `identity_matches:true` |
| `gh api repos/jirathip-dev/herdr-fleet --jq .full_name` | 0 | `run/logs/04c-gh-redirect-full-name.txt` | `jirathip-dev/canter` — the checkout's stale pre-rename origin URL is the SAME real repository; `origin_matches:false` is the pending rename (#106), not a wrong repository |

## 2. Deterministic plan (TWICE; byte determinism)

`herdr-fleet plan jirathip-dev/canter 105 --revision 7b876ab09053816e723f3646c479246b4dc1a9e2 --json`

| run | RAW exit | digest | log |
| --- | --- | --- | --- |
| 1 | 0 | `b7f88bad14dc6d3ec40bc53fa6bd566511a0ddeba2ae4ac36426dba94dca2c36` | `run/logs/05-plan-run1.json` |
| 2 | 0 | `b7f88bad14dc6d3ec40bc53fa6bd566511a0ddeba2ae4ac36426dba94dca2c36` | `run/logs/05-plan-run2.json` |

`cmp -s` over the two files: byte-identical (exit 0; identical file sha256
`ee50ea8e…c189`). Same `plan_id` (`hf_plan_fdf4853a66fae2e8`) and
`workflow_hash` (`a906f128…c01f`) in both.

**Recorded observation (gap):** the rendered plan carries `state_epoch: 0`
("no daemon state in this slice" — `src/plan.rs`). The daemon's apply refuses
that plan verbatim with `refusal.state.epoch` (section 4); a rendered plan can
therefore not be applied as-is. The operator rebound the epoch and re-derived
the content-addressed `plan_id` with the plan/daemon canonical rule
(sorted-key compact JSON, ASCII escapes, single trailing LF) — the derivation
was self-proved by recomputing the CLI's own digest and `plan_id` before use.

## 3. Daemon + exactly ONE bounded apply, with durable readback

- Seeding (operator boundary, no product surface exists — see section 6):
  ONE route grant `gr_1050a1b2c3d4e5f6` (repository/issue/revision/workflow/
  policy hashes/phase `worktree`/scope/caps/epoch 1/expiry) and its workflow
  instance `run-105` were written through the durable-state library API before
  the daemon started (the same seeding shape the repository's own e2e tests
  use). Log: `run/logs/07-seed.out`, exit 0.
- `herdr-fleet daemon run` (isolated state dir; socket from config): ready in
  <1 s on a real request exchange (not a bare connect); daemon log
  `run/logs/08-daemon-run.log`: `daemon.start`, mirrors rebuilt,
  `daemon.ready: state open; reconciled 0 interrupted claim(s)`.
- `herdr-fleet daemon status --json` — RAW exit 0, log
  `run/logs/09-daemon-status.json`: `active_grants:1, epoch:1, journal_seq:0,
  poisoned:false, schema_version:7`.
- `herdr-fleet capabilities --json` — RAW exit 0, log
  `run/logs/10-capabilities.json` (actor `herdr-fleet`, axis `forge`,
  `read_refs/read_issues/read_checks`).
- **The ONE bounded apply** — `apply` over the daemon socket (the CLI surface
  itself is read-only; `apply` is an RPC-only mutation, as documented), step
  `p2 = worktree_create` of the epoch-bound plan, idempotency key
  `ik_105-worktree-01`, request id `1a0500a2`:

  RAW exit 0, log `run/logs/13-apply-worktree.json`, response result:

      {"branch":"issue-105-native-run","contained":true,"dirty":false,
       "head":"7b876ab09053816e723f3646c479246b4dc1a9e2",
       "worktree":"<worktree>/run/worktrees105/issues-105"}

  The daemon executed the real effect (allowlisted `git worktree add -b
  issue-105-native-run <lane root>/issues-105 staging` in the disposable
  integration clone) and read the new worktree's head back before resolving.

- **Idempotent replay** (SAME request id `1a0500a2`, SAME key
  `ik_105-worktree-01`): RAW exit 0, log `run/logs/14-apply-replay.json`; the
  daemon returned the RECORDED response — byte-identical to the original
  (`cmp` exit 0; both sha256 `85dfc08e…b5d3`); the daemon's own log line:
  `{"event":"request.replay","message":"recorded response returned"}`.
- **At-most-once dispatch, proven against a live attempt:** the SAME key with
  a DIFFERENT request id (`1a0500a7`) is refused: `state.claim_reused` —
  `idempotency key "ik_105-worktree-01" already belongs to request 1a0500a2`
  (log `run/logs/25-apply-key-reuse.json`, RAW exit 1). The effect could not
  have re-run anyway (a duplicate `worktree_create` for an existing path fails
  closed), and the audit journal holds exactly ONE `worktree_create` intent
  for the key.
- **Durable readback (state, not console text):**
  - `journal.tail` RPC (`run/logs/16-journal-tail.json`, exit 0): audit chain
    `state.genesis` → `mutate.worktree_create` (grant `gr_1050a1b2c3d4e5f6`,
    `recorded_before_mutation:true`, plan_hash `62ceef67…6598` for the applied
    key) → `outcome.mutate.worktree_create`.
  - daemon-owned SQLite read (read-only; `run/logs/17-sqlite-readback.txt`):
    idempotency row `ik_105-worktree-01 | 1a0500a2 | apply | spent | resolved`;
    recorded typed outcome
    `{"schema":"hf-outcome/v1","status":"succeeded","idempotency_key":"ik_105-worktree-01","step_id":"p2","plan_id":"hf_plan_f9570ee293aa9ca1"}`;
    stored response == the raw response line we received (`True`, verified
    programmatically, `run/logs/11-apply-summary.txt`);
    instance row advanced `status=running, current_node=p2`; grant row `active`,
    epoch 1.
  - real effect on disk (`run/logs/18-git-readback.txt`): `git worktree list`
    shows the integration clone (`[staging]`) and the new lane worktree
    (`[issue-105-native-run]`), both at `7b876ab…`; the new worktree is clean
    (0 dirty lines); the integration clone's `staging` is unchanged.

## 4. Refusal / negative paths (typed codes, verbatim)

A happy-path-only run would not be evidence of a safe path; every refusal
below is a real socket exchange with the raw code recorded:

| probe | RAW exit | refusal (verbatim) | log |
| --- | --- | --- | --- |
| apply with the RENDERED plan (`state_epoch:0`) | 1 | `refusal.state.epoch` — "plan epoch 0 != live epoch 1; grants/plans die with their epoch" | `run/logs/12-apply-asrendered.json` |
| apply `p2` with the RENDERED step params (`{"scope":…}`) | 1 | `refusal.request.malformed` — "worktree_create requires a slug branch" (resolved as a durable `refused` outcome) | `run/logs/20-apply-badparams.json`, outcome in `run/logs/24-refusal-readback.txt` |
| apply a TAMPERED plan (params edited after the id derivation) | 1 | `refusal.plan.identity` — "plan_id … is not the content-derived id …" | `run/logs/21-apply-tamper.json` |
| adapter path: apply the `prompt` step (jcode row) | 1 | `refusal.binding.missing` — "the harness profile declares no provider/model binding; the prompt is refused (no default is inferred)" | `run/logs/22-apply-prompt.json` |
| unknown RPC method `grants.issue` (the surface has no grant issuance) | 1 | `refusal.malformed` — "rpc-request.method outside the closed method set" | `run/logs/23-unknown-method.json` |
| same key, different request id (at-most-once) | 1 | `state.claim_reused` | `run/logs/25-apply-key-reuse.json` |
| `daemon status` after SIGTERM (documented stale socket) | 1 | `daemon.stale` — "a stale daemon socket exists …; a fresh `daemon run` reclaims it" | `run/logs/30-daemon-status-after-stop.json` |

Fail-closed detail: the tampered-plan refusal leaves **no claim behind**
(0 idempotency rows, 0 audit rows for that key — identity is bound before any
journaling; `run/logs/26-tamper-absence.txt`). The pre-effect refusals that DO
journal an intent resolve as durable `refused` typed outcomes (db rows above).

## 5. Adapter identity (honest — NOT RUN for the identity canary)

- The merged adapter path is readable from this run: a `prompt` step with
  `kind=jcode` reaches the daemon's effect runner and refuses **before any
  harness process** with `refusal.binding.missing` (no provider/model binding
  is reachable from an apply request; no default is inferred) — see the
  section-4 row and `run/logs/22-apply-prompt.json`.
- The real adapter binary exists on this host: `jcode v0.84.0 (57d587899)`
  (`run/logs/27-adapter-credential-state.txt`). NO approved credentials exist:
  the provider config directory is empty (0 entries) and the adapter env
  allowlist (`PATH/HOME/LANG/LC_ALL/XDG_CONFIG_HOME/TMPDIR`) carries no
  credential-bearing variable at all.
- Therefore the real-canary identity probe was **NOT RUN**: there was no
  approved credential to run a real harness with, and no `provider`/`model`
  identity was returned, recorded, or inferred anywhere in this run. No
  fallback or default was substituted. The only executed coverage of the
  jcode envelope's returned-identity handling is the repository's
  fake-executable contract suite (`tests/harness_adapters.rs`, 44 passed incl.
  `jcode_json_envelope_transcript_and_returned_identity_are_surfaced` and
  `jcode_missing_provider_key_env_is_a_typed_credentials_refusal`) — cited
  explicitly as fake-binary contract tests, not as a real canary.

## 6. Manual-vs-native accounting

| step | performed by | evidence |
| --- | --- | --- |
| build the CLI/daemon (`--locked`, isolated target dir) | orchestrator | `run/logs/00-build-release.log` exit 0 |
| render/validate config, doctor, status, plan | **product CLI** | section 1–2 raw exits |
| write the config file (CLI never writes files, by design) | orchestrator | `run/logs/01-config-toml.copy` |
| choose repository identity + pin the acceptance revision (40-hex) | orchestrator | `run/logs/05-plan-summary.txt` |
| **issue the route grant + start the workflow instance** | orchestrator (no product surface: only `grants.list`/`grants.revoke` exist; `grants.issue` is refused as outside the closed method set) | section 3 seeding; `run/logs/23-unknown-method.json` |
| **rebind the plan epoch** (rendered plans hardcode `state_epoch:0`; apply refuses them) + re-derive `plan_id` | orchestrator | sections 2–4 |
| **supply effect params the renderer does not emit** (`worktree_create` needs `branch`+`worktree`; the rendered step carries only `{"scope":…}`) | orchestrator | `run/logs/20-apply-badparams.json` |
| start/stop the daemon process | orchestrator (CLI only runs it in the foreground) | `run/logs/08-daemon-run.log`, `run/logs/30-…json` |
| plan-digest + plan-id binding, grant/instance/epoch/policy revalidation, idempotency claim + pre-effect journal, the git effect, the typed outcome, replay/read-back | **product daemon** | sections 3–4 |
| declare admission caps + host-proof for the fan-out gate (values are client-attested, documented as such) | orchestrator | `run/params/prompt.json` |
| observe/read back state (`journal.tail`, `grants.list`, sqlite, git) | product RPC + operator read | section 3 |
| interpret outputs, write this report | orchestrator | this file |
| provide a real harness provider/model + credentials for an identity canary | human/orchestrator (not possible here — NOT RUN) | section 5 |

## 7. Gates (raw exit codes; `command | grep` was never PASS/FAIL)

- `just --list` — RAW exit 0 (recipes: build-release, check, ci, default, doc,
  fmt, fmt-check, lint, security, test; log `run/logs/32-just-list.log`).
- `just ci` — RAW exit **0** (log `run/logs/33-just-ci.log`): 16 test binaries,
  **344 passed / 0 failed**, cargo-deny ok, cargo-audit ok, scanner self-test +
  public-tree scan ok, gitleaks "no leaks found".
- per-recipe re-runs for per-recipe raw exits (warm caches; log
  `run/logs/34-per-recipe-summary.txt`):

| recipe | RAW exit | log |
| --- | --- | --- |
| `just fmt-check` | 0 | `run/logs/34-just-fmt-check.log` |
| `just check` | 0 | `run/logs/34-just-check.log` |
| `just lint` | 0 | `run/logs/34-just-lint.log` |
| `just test` | 0 | `run/logs/34-just-test.log` |
| `just doc` | 0 | `run/logs/34-just-doc.log` |
| `just build-release` | 0 | `run/logs/34-just-build-release.log` |
| `just security` | 0 | `run/logs/34-just-security.log` |
- Measurement note (disclosed): `gitleaks dir` scans the checked-out
  FILESYSTEM, not the git index. With the untracked lane scratch present it
  reports 2 `generic-api-key` findings on `ik_…` idempotency-key literals in
  the scratch request/params files (`run/logs/31b-gitleaks-quick.*`) — false
  positives on public identifiers that exist only in git-excluded scratch, not
  in the tracked tree. The gate run above was therefore measured with the
  untracked scratch outside the scan window (the tracked tree is exactly what
  CI checks out; the delivered head contains none of the scratch).

## 8. Boundaries / not verified

- NOT a replacement or autonomy claim: ONE bounded step on disposable local
  state; the rest of the doctrine workflow (harness start, prompt, collect,
  review, merge, cleanup) was not executed.
- Adapter identity canonical: NOT RUN (no approved credentials) — see section 5.
- No real harness process was spawned; the fake-binary contract tests are the
  only executed identity-handling coverage and are cited as such.
- No remote/GitHub mutation; the branch push (if performed) is the lane's own
  report delivery only. Remote sha readback is reported in the lane message.
- Host paths, credentials, and provider/model policy are confined to the
  untracked scratch and never appear in this report or any committed artifact.
- The `origin_matches:false` observation is the pre-rename checkout URL
  (issue #106), proven to be the same repository via the `gh` redirect proof.
