# .report-92.md — canter issue #92: target-scoped cutover proof and rollback plan

Lane: `issue-92-cutover-plan` (worktree `…/worktrees/herdr-fleet/issue-92-cutover-plan`),
branch of the same name, base `origin/staging` @ `7df64d9ec751c86822039c613c48587f5c2f016b`
(the merged #83/#84/#85/#90 head, verified before work). Public repository; commit wording
`Refs #92`; delivery is the pushed branch — no PR, no issue mutation, no cutover step executed.

Raw exits and logs live host-local, outside the tracked tree, under
`/tmp/cutover-92-logs/` and are referred to by file name below.

## 1. Files changed (artifact diff)

`git diff --stat origin/staging...HEAD` at the implementation commit `5e965f6` (this report
is the only file added on top of it; the delivered-head stat in the final message includes
this file):

```
 docs/operations/cutover-92.md   | 336 +++++++++++++++++++++
 scripts/cutover-92-dryrun.py    | 630 +++++++++++++++++++++++++++++++
 scripts/cutover-92-rehearsal.sh | 468 ++++++++++++++++++++++++++++++
 scripts/test-cutover-92.py      | 796 +++++++++++++++++++++++++++++++
 4 files changed, 2230 insertions(+)
```

- **NEW `docs/operations/cutover-92.md`** — the reviewed plan: scope (one queue, one host
  role, all concrete identities as binding keys), the current-supervisor inventory by role,
  the private `cutover-target/v1` descriptor, the target Canter-owned mechanism per the
  merged work, ordered steps S0–S6 with the verification that must pass after each, rollback
  R1–R6 with exact restore commands, the blast-radius statement, the approval-gate wording
  naming the owner as sole approver, and an explicit "what this plan does not prove" section.
- **NEW `scripts/cutover-92-dryrun.py`** (stdlib only) — the zero-mutation precondition
  proof: (a) artifacts/units exist as the plan describes, (b) exactly one supervisor process
  is running and its identity matches the descriptor and the recorded identity, (c) every
  rollback command substitutes, passes `bash -n` (parsed, never executed), resolves to an
  existing binary, and every restore input exists with the recorded sha256, (d) the target
  mechanism answers read-only probes inside a fixed allowlist. The pre/post fingerprint over
  every inspected artifact and the observed process identity must be identical
  (`nomutation`), and the summary line is machine-checkable.
- **NEW `scripts/cutover-92-rehearsal.sh`** — the rollback rehearsal against a disposable
  fixture: plants a fixture unit + fixture supervisor + fixture descriptor in a `mktemp -d`
  sandbox, runs the dry-run against it, performs a simulated cutover **inside the sandbox**,
  executes the plan's `sandbox-runnable` rollback commands for real, prints the
  `owner-executed` ones as `SIMULATED`, and re-verifies bytes, identity and the dry-run.
  Containment is enforced at runtime (root must be a fresh temp path; every touched path and
  every path inside an executed command must live under it; the stopped pid must be its own
  process).
- **NEW `scripts/test-cutover-92.py`** — self-tests: 22 mutation probes proving each dry-run
  check fails closed (including a deliberately *writing* probe that must turn `nomutation`
  red), the external file/process fingerprint proof, the rehearsal's containment RED probe,
  and source-level static guards.

Ownership respected: `src/commands.rs`, `src/state.rs`, `src/daemon.rs`, `src/tui/**`,
`src/board.rs`, `src/queue_preview.rs` and every existing test are byte-unchanged (they are
not in the diff above).

## 2. Dry-run evidence (AC2)

```
$ python3 scripts/cutover-92-dryrun.py --target <fixture descriptor>   # exit 0
PASS: descriptor.read … PASS: artifacts.unit … PASS: supervisor.running — 1 process(es) match the declared pattern
PASS: supervisor.identity_descriptor … PASS: supervisor.identity_record …
PASS: rollback.commands_syntax — bash -n clean for every command
PASS: rollback.commands_resolve — every command word resolves
PASS: rollback.binaries — all declared binaries resolve
PASS: rollback.inputs — all restore inputs present and hash-matched
PASS: target.probe[0] — `--version` exit=0
PASS: target.probe[1] — `status --json` exit=0 envelope schema='hf-output/v1'
PASS: nomutation — sha256(file-bytes|size|mtime_ns)+sha256(process-identity) unchanged across the run
SUMMARY result=ok checks=22 passed=22 failed=0 mutations=0 fingerprint=bcf4e732ff1e
```

Raw exit **0**; full output `dryrun-standalone.log`; the machine-checkable JSON document
(`--json`, exit **0**) is `dryrun-standalone-json.log`. The descriptor used is the synthetic
fixture host built and kept by the rehearsal (`CUTOVER92_REHEARSAL_KEEP=1`,
`rehearsal-keep.log`); the live supervisor's identity is never read, named or written by any
of these runs.

Zero-mutation proof, three independent witnesses:

1. the dry-run's own `nomutation` check (same fingerprint before and after the checks);
2. the self-test's external comparison — the fixture's watched files (unit, backup copy,
   identity record, canter stub, config) and the observed process identity are byte-identical
   before and after every dry-run invocation (`dry-run runs left every watched fixture file
   unchanged (external sha256 proof)`), and unchanged again after the whole suite;
3. the source-level AST guard — the dry-run has no write/kill/chmod/remove/rename call, no
   `open(..., "w"/"a"/"x"/"+")`, no `shell=True`, exactly one `subprocess.run` call site, and
   it never names `launchctl`/`systemctl` (it only runs `ps`, `bash -n` and the allow-listed
   canter probes).

## 3. Rollback rehearsal evidence (AC3)

```
$ bash scripts/cutover-92-rehearsal.sh                                  # exit 0
PASS: dry-run exit 0 against the intact fixture
PASS: dry-run summary reports result=ok and mutations=0
PASS: running identity matches the recorded identity before any change
PASS: dry-run fails while the supervisor is down and the unit is replaced (exit 1)
PASS: the failing check is named: supervisor.running
    executing [R1] (sandbox): cp -p <sandbox>/host/backup/…plist <sandbox>/host/units/…plist
    executing [R2] (sandbox): cmp -s <sandbox>/host/backup/…plist <sandbox>/host/units/…plist
    SIMULATED (owner-executed on host, NOT run here) [R3]: launchctl bootstrap gui/<uid> <unit>
    SIMULATED (owner-executed on host, NOT run here) [R4]: launchctl enable gui/<uid>/<label>
    SIMULATED (owner-executed on host, NOT run here) [R5]: launchctl kickstart -k gui/<uid>/<label>
PASS: rollback path exercised: 2 executed, 3 simulated
PASS: restored unit bytes are byte-identical (sha256 b4bcbc3f…)
PASS: post-restart supervisor identity matches the baseline (argv sha256 a874609f…)
PASS: dry-run exit 0 after the rollback (preconditions hold again)
REHEARSAL result=ok steps=8 verifications=9 failures=0 sandbox=/var/folders/…/cutover-92-rehearsal.6F4vTw live_paths_touched=0
```

Raw exit **0**; full output `rehearsal.log`. It is provably harmless: the sandbox is a fresh
`mktemp -d` path under the OS temp dir (an existing root is refused, a root outside the temp
dir is refused — asserted by the containment RED probe), the only stopped process is the
fixture the script started itself (pid file + command-line check under the same root), the
executed rollback commands are path-guarded to the sandbox, and the owner-executed
service-manager commands are only printed. `live_paths_touched=0`; no straggler process
exists after the run (`rehearsal leaves no fixture process behind — 0 straggler(s)`).

## 4. Self-test (mutation probes)

```
$ python3 scripts/test-cutover-92.py                                    # exit 0
SELF-TEST SUMMARY result=ok checks=43 passed=43 failed=0 skipped=0
```

Raw exit **0**; log `selftest.log`. Included RED probes (each asserts exit 1 **and** the named
failing check): `artifacts.unit`, `artifacts.unit_label`, `artifacts.unit_program`,
`artifacts.canter_binary`, `artifacts.canter_config`, `descriptor.schema`,
`descriptor.paths_absolute`, `descriptor.placeholders`, `supervisor.running`,
`supervisor.identity_descriptor`, `supervisor.identity_record` (missing and mismatched),
`rollback.commands_syntax`, `rollback.commands_resolve`, `rollback.binaries`,
`rollback.inputs` (missing and hash-mismatch), `target.probe` (non-zero exit, disallowed
argv refused before execution, bad `hf-output/v1` envelope), `supervisor.running` for both
zero and two matching processes, and `nomutation` — the last one
driven by a probe that really writes to a file, plus the assertion that the probe's target
file did change (`the nomutation probe actually mutated its target`).

## 5. Gate evidence

Isolation: `CARGO_TARGET_DIR=<worktree>/target-92`, `CARGO_BUILD_JOBS=4` (the brief's lane
isolation). One deliberate exception, below.

| Command | Raw exit | Log |
| --- | --- | --- |
| `just --list` (once, 10 recipes) | 0 | `just-list.log` |
| `just ci` — attempt 1 (staged index) | 1 | `just-ci-staged.log` |
| `just ci` — attempt 2 (implementation commit `5e965f6`) | 101 | `just-ci-final.log` |
| focused rerun of the one failing test (bounded diagnostic) | 0 | `focused-daemon-rpc-flake.log` |
| `just ci` — attempt 3 (same head `5e965f6`) | 0 | `just-ci-attempt3.log` |
| `python3 scripts/test-build-archive.py` | 0 | `script-test-build-archive.log` |
| `python3 scripts/check-contract-fixtures.py` | 0 | `script-check-contract-fixtures.log` |
| `python3 scripts/test-check-public-tree.py` | 0 | `script-test-check-public-tree.log` |
| `python3 scripts/check-public-tree.py .` (delivered head, index) | 0 | `scan-delivered-head.log` |
| CI documentation-link check (replicated locally) | 0 | `docs-link-check.log` |

Attempt history (all raw; nothing hidden):

- **Attempt 1 — raw exit 1, `security` stage.** The public-tree scanner reported
  `RULE-ABS-PATH-UNIX<TAB>scripts/test-cutover-92.py`: my self-test's own guard contained the
  banned absolute-home-path literal it searches for. Fixed the way the scanner itself does it
  (the fragments are assembled at runtime: `"/" + "Users" + "/"`), re-ran the scan → exit 0
  (`scan-after-fix.log`). The gate bit on the lane's own artifact, which is the intended
  behaviour.
- **Attempt 2 — raw exit 101, `test` stage.** `tests/daemon_rpc.rs::non_draining_subscriber_is_disconnected_under_backpressure`
  panicked at `tests/daemon_rpc.rs:123` with `RpcError { code: "client.read", message:
  "Resource temporarily unavailable (os error 35)" }` — the 15 s daemon-client read deadline,
  under host load average 164 (other lanes' work). The failing test file is byte-unchanged by
  this slice (the diff adds docs and three new `scripts/` files only). A bounded focused rerun
  of exactly that test passed (exit 0, 68 s). This is the same load-sensitive flake the #85
  lane disclosed (`tests/daemon_rpc.rs`, same signature); it is NOT proven pre-existing by this
  lane — it is reported as an open attribution, not fixed.
- **Attempt 3 — raw exit 0**, full chain: `fmt-check → check → lint → test → doc →
  build-release → security` (scanner self-test, scan, `cargo deny check`, `cargo audit`,
  gitleaks `no leaks found`). 28 `test result: ok`, 0 `test result: FAILED`.
- **`test-build-archive.py` target-dir note.** That pre-existing script hardcodes
  `<repo>/target/release/{canter,herdr-fleet}` and does not honour `CARGO_TARGET_DIR`, so it
  was run with the variable unset (its own documented default) — the build it triggered landed
  in `<worktree>/target/` (ignored, untracked). No source change was made for this: the lane
  must not modify existing tests, and the script's own `PASS`/`FAIL` lines above are its real
  output. Every other command ran against `target-92` as the lane isolation requires.

## 6. Live-supervisor safety (AC4)

- **No service-manager mutation was executed by this lane.** The only
  service-manager-adjacent commands run anywhere in the lane were read-only observations
  (`launchctl list`, `crontab -l`, `ps`); no `launchctl bootout/bootstrap/enable/disable/
  kickstart`, no `crontab -e`, and no write outside the worktree's tracked tree and the OS
  temp dir was performed.
- **The scripts cannot execute one.** The dry-run never names a service manager (static
  guard) and only ever runs `ps`, `bash -n` and allow-listed read-only `canter` probes. The
  rehearsal executes only descriptor commands classified `sandbox-runnable`, behind a
  runtime path guard (static guard asserts the guard precedes execution), and prints
  `owner-executed` commands as `SIMULATED`; its containment guard refused a non-temp root in
  the RED probe before creating anything.
- **The plan itself executes nothing.** The disable commands exist only as text in the
  reviewed plan; the gate (section 9 of the plan) names the owner as sole approver.

## 7. Acceptance points, each named

1. **Target-scoped, reversible plan with exact restore steps and a blast-radius statement** —
   `docs/operations/cutover-92.md` sections 1–3 (scope + inventory), 6 (ordered steps, each
   with its verification), 7 (rollback R1–R6 with exact commands), 8 (blast radius).
2. **A dry-run proves preconditions and rollback resolution without mutation** —
   `scripts/cutover-92-dryrun.py` (output above; zero-mutation proven three ways).
3. **The rollback path is rehearsed against a disposable fixture with captured evidence** —
   `scripts/cutover-92-rehearsal.sh` (`rehearsal.log`, exit 0; execution of the real restore
   commands in the sandbox plus the byte/identity re-verification).
4. **The approval gate is documented and nothing in the lane disables the live supervisor** —
   plan section 9 (owner as sole approver) and section 6 above.
5. **The plan states honestly what remains unproven and does not claim full replacement** —
   plan section 5 ("Not replaced by merged work") and section 11; `PENDING INTEGRATION`
   below.

## 8. PENDING INTEGRATION

1. **`src/commands.rs` — no read-only CLI verb renders the #84 preview document.** Exact
   change: a `canter queue preview …` verb that renders the `hf-queue-preview/v1` document
   and prints its digest (`--json`), mutating nothing. Why: today the preview is
   library-only, so an operator cannot obtain the `--request` file and `--confirm-digest`
   value through the product surface, and plan step S5's operator path depends on it.
2. **`src/daemon.rs` (+ `src/commands.rs` wiring) — no canter-owned continuation.** Exact
   change: the #95/#96 slices (durable event-driven reconciliation with a bounded timer
   fallback + one bounded orchestrator continuation) driving the next eligible issue. Why:
   the legacy supervisor's rearm/continuation role has no canter equivalent merged, so after
   a disable the cutover window is operator-driven; the plan states this limit and keeps the
   window bounded by rollback, but automatic continuation cannot be claimed until #95/#96
   land.

## 9. What is NOT proven (plainly)

- **No cutover behaviour.** Nothing was executed against the live supervisor; the disable,
  the post-disable queue proof (S5) and the rollback against the real host are unproven by
  construction — this lane stops at the owner's gate.
- **The live carrier set is not confirmed in this lane.** The read-only host observation
  found no per-user crontab entry and no process naming a fleet-ops supervisor script; the
  plan's inventory is therefore carriers-by-key and the exact carriers (and their
  disable/restore strings) must be confirmed by the owner in the private descriptor at the
  gate.
- **The service-manager steps are unexecuted.** `launchctl` commands are syntax-checked and
  binary-resolved only; the rehearsal exercises the file-restore half plus a fixture
  lifecycle, not `launchctl` itself.
- **Automatic continuation/rearm is not replaced** (#95/#96/#97/#98, #86, #91 unmerged);
  the window is operator-driven, as the plan and the approval wording state.
- **Pre-existing, untouched:** the `harness_lanes.unwrap_or(0)` gap in the #48 admission gate
  (already reported open by the #85 lane) and the load-sensitive `daemon_rpc` client
  deadline flake disclosed by the #85 lane are neither fixed nor re-adjudicated here.

## 10. Closeout

- **Candidate head (every gate above ran against it):** `5e965f6cb5363a04a7b82bc2f6b57b740e59399b`
  (`Cutover plan and rollback rehearsal for one Mac queue … (Refs #92)`), base
  `origin/staging` @ `7df64d9ec751c86822039c613c48587f5c2f016b`.
- **This report** is committed on top of it as a documentation-only follow-up commit
  (also `Refs #92`); the delivered head is that commit — its SHA, the
  `git status --porcelain` at closeout and the pushed remote SHA readback are stated in the
  lane's final message and verified by
  `git ls-remote origin refs/heads/issue-92-cutover-plan`.
- Branch pushed: yes; no PR opened, no issue mutated, no cutover step executed.
