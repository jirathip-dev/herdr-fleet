# Report — issue #3 contract artifacts (docs + schemas + fixtures)

Branch: `issue-3-contracts` (base origin/staging b0cac966c04872136894e2e8975a1970faa019f7).
Scope: contract artifacts ONLY. `src/`, `Cargo.*`, `tests/`, `justfile`, and
`docs/architecture/README.md` untouched (lane fence). No PR opened; push
only. Refs #3 / #1 — no Fixes/Closes wording.

## Deliverables -> acceptance criteria map

| #3 deliverable | Where | ACs |
| --- | --- | --- |
| Capability map: RETAIN/MERGE/SEPARATE/RETIRE + rationale + dependency order | `docs/contracts/capability-map.md` (27 capabilities, C1-C27) | AC1: every capability has exactly one owner + dependency position |
| Versioned specs + fixtures: XDG TOML config + optional policy overlay | `docs/contracts/spec-config.md`; families `hf-config/v1`, `hf-policy/v1` | AC2 |
| Identities/observations/errors/exit codes/partial-freshness/human-vs-JSON | `docs/contracts/spec-cli.md`; families `hf-output/v1`, `hf-error/v1`, `hf-observation/v1` | AC2 |
| Deterministic plans + digests, route grants, state epochs, idempotency keys, typed outcomes | `docs/contracts/spec-plans.md`; families `hf-plan/v1`, `hf-grant/v1`, `hf-epoch/v1`, `hf-outcome/v1` | AC2, AC3 (grant binds repository, issue+acceptance revision, workflow/policy hashes, phase, scope, caps, expiry, epoch), AC7 (restore -> epoch rotation; interrupted work ambiguous) |
| Closed typed workflow DAGs + canonical hashing/serialization | `docs/contracts/spec-workflow.md`; family `hf-workflow/v1` (canonical bytes + pinned digest) | AC2, AC5 non-downgrade (no risk field on nodes) |
| Daemon request/response + JSONL events | `docs/contracts/spec-daemon.md`; families `hf-rpc-request/v1`, `hf-rpc-response/v1`, `hf-event/v1` | AC2 |
| SQLite migration/journal/audit + backup/restore + retention | `docs/contracts/spec-state.md`; families `hf-migration/v1`, `hf-audit/v1` (JSONL) | AC2, AC6 (mutate.* requires recorded_before_mutation=true — fail closed, schema-enforced), AC7 |
| Harness + forge capability negotiation | `docs/contracts/spec-capabilities.md`; family `hf-capability/v1` | AC2 |
| Review evidence contract | `docs/contracts/spec-review-evidence.md`; family `hf-evidence/v1` | AC4 (feature head SHA + integration-base SHA + workflow hash + policy hash; invalidation rule) |
| Trust model | `docs/contracts/trust-model.md` (T1-T7) | issue bullets |
| Daemon-owned fail-closed target/risk model | `docs/contracts/risk-model.md` (target table, UNKNOWN -> production+destructive, non-downgrade rule) | AC5 |
| Corral archaeology matrix | `docs/contracts/corral-archaeology.md` — 10 candidates (ADAPT/LEARN/REJECT) with public commit/path/test + Apache-2.0 OR MIT provenance at both named SHAs | AC8 (no private names/paths), AC9 (pending independent review stated before any adapted code) |
| Herdr + `gh` tested compatibility policy | `docs/contracts/compatibility.md` — negotiation + declared minimum/current + `doctor` never installs/starts/stops/upgrades | AC10 (measured version facts vs [awaiting-evidence] markers) |
| Benchmark corpus + method | `docs/contracts/benchmarks.md` — 9 synthetic offline scenarios, no universal network-latency promises | AC10 |
| Design-vs-evidence distinction | `docs/contracts/README.md` evidence legend + per-doc markers | AC10 |
| Malformed/unknown-version refusal probes | `scripts/check-contract-fixtures.py` + `scripts/test-check-contract-fixtures.py` (129 checks) + `schemas/fixtures/manifest.jsonl` (61 rows) | AC2 refusal fixtures |

## Registry + fixtures

- `docs/contracts/schema-registry.md`: 17 schema families, each with
  identifier, compatibility rule, canonical bytes/hash rule, and fixture
  set; embedded scalar formats table (identities, digests, idempotency
  keys, epochs, timestamps).
- `schemas/fixtures/`: 61 synthetic fixture files (17 families x valid +
  malformed + unknown-version, plus non-canonical and partial/error
  variants). No downstream identifiers or machine paths anywhere.
- Probe (stdlib-only, deterministic exit codes) validates manifest
  expectations incl. known-answer sha256 digests pinned for canonical
  `hf-plan/v1` and `hf-workflow/v1` documents; non-canonical bytes are
  refused. Self-test proves each refusal bites on tampered copies and
  enforces registry/manifest/doc coherence.
- Wiring the probe into `justfile`/CI is deferred to the path-owning lane
  (fence); documented in `docs/contracts/README.md` (Fixture checks).

## Verification (raw)

Fixture gates on this branch head:

```
python3 scripts/check-contract-fixtures.py .   -> "61 manifest expectation(s) held (61 rows)"  EXIT=0
python3 scripts/test-check-contract-fixtures.py -> "129 discrimination/coherence checks passed" EXIT=0
python3 scripts/check-public-tree.py .          -> no violations                                           EXIT=0
documentation-link replica (docs/**/*.md)       -> "documentation-link check: clean"                       EXIT=0
```

Full local aggregate gate (`just ci` = fmt-check -> check -> lint -> test ->
doc -> build-release -> security), raw exit code:

```
just ci > /tmp/issue3-just-ci.log 2>&1; echo EXIT=$?
EXIT=0
```

Inside the log: scanner self-test 21 tests OK; public-tree scan passed;
cargo-deny ok (advisories/bans/licenses/sources); cargo-audit no
vulnerabilities; gitleaks no leaks found. The gate ran on the fully staged
tree; `.report-3.md` was added afterwards and the index scans were re-run
on the final staged tree (public-tree scan passed, gitleaks no leaks,
fixture gates green) before commit.

## Files touched (new unless noted)

- `docs/contracts/` — README.md (index + deliverable map), capability-map.md,
  schema-registry.md, spec-config.md, spec-cli.md, spec-plans.md,
  spec-workflow.md, spec-daemon.md, spec-state.md, spec-capabilities.md,
  spec-review-evidence.md, trust-model.md, risk-model.md, corral-archaeology.md,
  compatibility.md, benchmarks.md
- `docs/ARCHITECTURE.md` — "Contract artifacts (issue #3)" section + link
- `schemas/fixtures/` — 17 family dirs, 61 fixtures + manifest.jsonl
- `scripts/check-contract-fixtures.py`, `scripts/test-check-contract-fixtures.py`
- `.report-3.md` (this file)

## Not verified / deferred

- Hosted CI (policy job incl. documentation-link check, rust-ubuntu,
  rust-macos) runs only after the orchestrator opens the PR; locally
  replicated the link check (clean).
- Corral matrix independent review (AC9) and all [awaiting-evidence]
  compatibility/performance claims require later slices with real
  implementations.
