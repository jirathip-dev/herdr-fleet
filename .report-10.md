# Slice report — issue #10: release readiness (portable artifacts, provenance, clean-host proof, 1.0 RC contract)

Lane: `issue-10-release` (herdr-fleet worktree). Base: `4e7d00e415cef61d236f595c0f4f32e36de068e0`
(origin/staging). Gates measured at committed head `fafd422c1056b4bd4e55ecbe3a8484e0a89da019`;
final aggregate `just ci` re-run at the final committed head (report commit included).

Commits on this lane (oldest first):

1. `0e3c95f` Version/schema facts exposed on --version for the release provenance chain (Refs #10)
2. `fea6735` Release readiness: archive builder + provenance verifier, baseline measure/check, clean-host verification scripts (Refs #10)
3. `e9e5c4f` Release readiness docs: RELEASING active for machinery, compatibility probe map, measured baseline, README/skill truthfulness (Refs #10)
4. `73ed7dd` Fix clippy redundant-static-lifetimes in migration-chain facts (Refs #10)
5. `…` Archive self-test fixes: reuse the builder's Cargo.lock parser; SBOM count mirrors Cargo.lock incl. root (Refs #10)
6. `fafd422` Fix RELEASING doc link depth for the archive builder script (Refs #10)
7. `…` Final: this report (Refs #10)

## Acceptance-criteria map (issue #10 body — first 5 ACs are machine-provable in-repo; execution human-gated)

- **AC1 provenance chain (machine-proven on this host)** —
  `scripts/build-archive.py` builds a deterministic platform archive from an
  EXACT recorded source ref + version (builder refuses wrong refs, dirty
  trees, version mismatches, unknown platforms): documented layout, adjacent
  archive `.sha256`, per-file `SHA256SUMS`, offline SPDX-2.3 SBOM derived
  from `Cargo.lock` (46 packages incl. root), and a `release-provenance/v1`
  record binding source ref + binary-reported schema facts (state schema v4,
  migration chain m0001..m0004, all 17 document families) + file digests +
  SBOM digest. `verify` re-extracts and machine-checks the whole chain and
  the archived binary's own `--version` against the record. End-to-end
  proof: `scripts/test-build-archive.py` (build at the recorded HEAD on the
  CURRENT host, linux-x86_64, disposable output — plus byte-identical
  rebuild proof). GitHub/Sigstore attestation and the other three platform
  archives remain **human-gated release execution**.
- **AC2 clean-host verification (scripts + command rows committed; runs
  human-gated)** — `scripts/clean-host-verify.sh` +
  `scripts/clean-host-probe.py` document and drive the fresh-host checks
  (help/version incl. schema facts, synthetic config init/validate,
  `service install/status/uninstall-plan` rendering, disposable-temp daemon
  round trip: boot/status/epoch, `backup.create` → `restore.begin` with
  epoch rotation + post-restore durability, synthetic `hf-schedule/v1`
  lifecycle create/list/pause/resume/delete, fail-closed `apply` refusal,
  one `hf-event/v1` snapshot line). Real runs on fresh macOS/Linux
  maintainer hosts are **human-gated**; `scripts/test-clean-host-probe.py`
  exercises the identical flow fixture-level on this host (all PASS).
- **AC3 real authenticated smokes (human-gated, not run)** — reference
  table kept consistent in docs/contracts/compatibility.md (Hermes Agent
  0.21.0 / Claude Code 2.1.263 / Codex 0.153.4 exact versions, issue #7 AC6
  pattern); this lane documents the mapping and adds no real smokes.
- **AC4 compat measurement — honest rows only** — compatibility.md gained
  the "Capability-probe mapping" tying every declared row to its in-tree
  probe (`observe.rs HERDR_MINIMUM/GH_MINIMUM`, `adapters.rs
  official_specs()`, `tests/cli_readonly.rs`, `tests/harness_adapters.rs`,
  fixture oracle); no [awaiting-evidence] marker was removed because this
  lane measures no new live version facts (explicitly stated in the doc).
- **AC5 baselines — measured on this host** — `scripts/measure-baseline.py`
  (measure/check) + committed table
  `docs/contracts/baseline-linux-x86_64.csv` + table in benchmarks.md:
  pure-core offline scenarios (help, version, capabilities,
  config-validate/show, service-install-plan), N=30, p50/p95 ms + peak RSS,
  budgets `max(p95 x 2.0, 25 ms)`, every run exit recorded — non-zero runs
  fail loudly (partial failures never hidden); check is opt-in on the
  reference host class, deliberately NOT a CI gate (shared-runner flake
  caveat documented).
- Version/schema policy wording from the issue (SemVer; explicit schema
  versions; latest-minor-receives-fixes; breaking stable schema changes
  require migrations + major; soak-clock reset rule) is now in
  RELEASING.md, linked from WORKFLOW/README. Schema facts made queryable by
  the binary (`--version`, bound by lib unit test + cli_smoke + builder).
- Release issue/PR contract, GitHub Releases sole canonical channel, no
  self-update / no package-manager promises, 1.0 RC + 30-day soak flow:
  documented in RELEASING.md (human-gated by contract; no workflows/tags).

## Raw gate results (measured at committed head fafd422; aggregate re-run at final head)

| Gate | Command | Exit | Log |
| --- | --- | --- | --- |
| list | `just --list` | 0 | /tmp/issue10-just-list.log |
| fmt-check | `just fmt-check` | 0 | /tmp/issue10-fmt.log |
| check | `just check` | 0 | /tmp/issue10-check.log |
| lint | `just lint` | 0 | /tmp/issue10-lint.log |
| test | `just test` (250 passed, 11 suites, 0 failed) | 0 | /tmp/issue10-test.log |
| doc | `just doc` | 0 | /tmp/issue10-doc.log |
| build-release | `just build-release` | 0 | /tmp/issue10-build-release.log |
| security | `just security` (scanner self-test + scan, cargo deny, cargo audit, gitleaks) | 0 | /tmp/issue10-security.log |
| fixture oracle | `python3 scripts/check-contract-fixtures.py .` | 0 (65 manifest rows held) | /tmp/issue10-oracle.log |
| oracle self-test | `python3 scripts/test-check-contract-fixtures.py` | 0 (136 checks) | /tmp/issue10-oracle-selftest.log |
| archive self-test | `python3 scripts/test-build-archive.py` | 0 | /tmp/issue10-test-build-archive.log |
| baseline self-test | `python3 scripts/test-measure-baseline.py` | 0 | /tmp/issue10-test-baseline.log |
| clean-host self-test | `python3 scripts/test-clean-host-probe.py` | 0 | /tmp/issue10-test-cleanhost.log |
| baseline check | `measure-baseline.py check` (same host class) | 0 | /tmp/issue10-baseline-check.log |
| doc-link policy (simulated hosted check) | policy-job link checker over README + docs | 0 | — |
| FINAL aggregate | `just ci` at the final committed head | 0 | /tmp/issue10-just-ci.log |

## Human-gated items (never run on this lane)

- Real clean-host installs of the daemon/service on fresh macOS/Linux
  maintainer hosts (AC2) and real authenticated Hermes/Claude Code/Codex
  smokes (AC3, issue #7 AC6) — scripts + rows committed, fixture-level
  equivalents self-tested here only.
- Release execution: main promotion, tags/archives-for-release, GitHub
  Releases upload, Sigstore/GitHub provenance attestation, 1.0 RC + 30-day
  soak, package publication, repository archives. No such action exists in
  or runs from this repository.

## Docs updated (or exact docs-checked reason)

- `docs/RELEASING.md` — made ACTIVE for the in-repo readiness machinery with
  release-execution steps explicitly human-gated; version/schema policy,
  archive layout/checksum/SBOM/provenance contract, documented command
  rows, release issue/PR contract, 1.0 RC + soak flow, rollback notes.
- `docs/WORKFLOW.md` — Releases section + link updated (readiness active;
  execution human-gated).
- `docs/DEVELOPMENT.md` — release-readiness tooling + self-test rows added
  (canonical gate home).
- `docs/contracts/compatibility.md` — capability-probe mapping (AC4);
  honesty statement (no [awaiting-evidence] markers removed).
- `docs/contracts/benchmarks.md` — measured-baseline section with host
  caveats; awaiting-evidence scope narrowed to what this lane actually
  measured; corpus S1-S8 remain awaiting-evidence.
- `docs/contracts/baseline-linux-x86_64.csv` — new committed measured table.
- `README.md` — roadmap #10 wording, docs index, RELEASING description.
- `skills/herdr-fleet/SKILL.md` — version bump 1.5.0 → 1.6.0, #3–#10
  wording, release-readiness link-not-duplicate section.
- `src/state.rs` (+lib.rs/main.rs/commands.rs/tests/cli_smoke.rs) — the
  small code exposure making schema facts queryable from `--version`.
- Not changed (docs-checked): CHANGELOG.md (prior slices #3–#9 did not add
  entries; bootstrap section untouched), docs/contracts/README.md
  (issue-#3-scoped deliverable map; its compatibility/benchmarks links
  still resolve), docs/contracts/spec-*.md, schema-registry.md,
  capability-map.md, docs/decisions/* (no decision changed), ADRs.
- **AGENTS.md: DEFERRED (protected instruction file — not edited per lane
  fence).** Intended replacement if the orchestrator routes a follow-up:
  ownership-map rows are stale for this slice — "CLI entry (help/version
  only) | src/main.rs" no longer reflects the command surface, the
  scripts row should list the release-readiness scripts
  (`scripts/build-archive.py`, `scripts/measure-baseline.py`,
  `scripts/clean-host-verify.sh`, `scripts/clean-host-probe.py` + their
  `test-*.py`), and the mandatory-gates text could note that release
  readiness scripts self-test outside `just ci` (docs/DEVELOPMENT.md is the
  canonical home).

## Not verifiable on this host / follow-ups (Refs #10)

- Real clean-host runs (AC2), real authenticated smokes (AC3), GitHub
  Release upload/attestation, darwin archives and macOS daemon/socket
  behavior (fixture-level checks are platform-portable stdlib; macOS host
  gate is the maintainer clean-host run), 30-day soak.
- Shell completions/manpages generation does not exist in the bootstrap
  (hand-rolled parser, no completion framework); noted as a pre-1.0
  release-packaging follow-up in RELEASING.md.
- The fake-adapter benchmark harness for corpus scenarios S1–S8 remains a
  follow-up (benchmarks.md stays honest: only the pure-core command set is
  measured).
- AC1 archive byte-reproducibility is proven for identical inputs on this
  host; cross-toolchain binary reproducibility is explicitly not claimed
  (verification maps artifacts to provenance instead).
