# Slice report — issue #9: lifecycle (schedules, admission, cleanup archive, remote transport, cold-boot recovery)

Lane: `issue-9-lifecycle` (herdr-fleet worktree). Base: `21cdf43b`.
Commits on this lane (oldest first):

1. `84e5f7d` Lifecycle foundation: m0004 schedule payloads, hf-schedule/v1 family, schedule eval + admission core (Refs #9)
2. `…` Daemon lifecycle RPCs + boot schedule recovery, backup retention pruning, fan-out admission gate (Refs #9)
3. `26cbfa5` Cleanup archive/salvage + symlink refusal, system-SSH remote transport contract (Refs #9)
4. `f751770` Lifecycle wire probes: schedule RPC/eval/boot recovery, admission refusals, cleanup archive/symlink (Refs #9)
5. `…` docs/skill/fixtures (Refs #9)

## Acceptance-criteria map (issue #9 body)

- **AC1 fan-out admission** — `refusal.admission.*` gates (`proof_missing`,
  `proof_stale`, `cap_missing`, `cap_global`, `cap_repository`,
  `cap_harness`, `monorepo_overlap`) enforced by the daemon before any
  intent is journaled for `harness_start`/`prompt`; global + per-repository
  caps count the daemon's own durable active lanes; per-harness axis is
  caller-attested (documented boundary, spec-lifecycle.md §4). Probes:
  `admission_refuses_missing_or_stale_proof_and_missing_caps_over_the_wire`
  (tests/daemon_lifecycle.rs) + pure unit tests (src/lifecycle.rs).
- **AC2 bounded non-destructive recurring grants** — `hf-schedule/v1`
  document pins repository/issue/workflow+policy hash/phase/scope/caps
  (`["read"]` only)/expiry/anchor/every_secs; schedule rows durable in
  SQLite (migration m0004); evaluation single-flight, coalesced,
  anchor-aligned, no backlog replay; refusals park the schedule in an
  explicit terminal state with a journaled reason. Probes:
  `schedule_create_list_pause_resume_delete_round_trip`,
  `schedule_evaluate_fires_due_once_and_expired_schedules_park`,
  unit tests `next_window_*`, `coalesces_*`, `expired_schedule_parks_*`
  (src/lifecycle.rs + tests/daemon_lifecycle.rs).
- **AC3/AC4 pause/resume/rearm durable; schedules/stale approvals never
  lift** — `schedules.pause/resume` are durable row flags; evaluation
  never enables; restart test
  `paused_schedule_and_instance_survive_restart_and_boot_recovery` proves
  pause survives and boot recovery cannot fire a paused schedule; instance
  pause/resume durability with single-use digests stays on the #8 engine
  gates (state unit tests).
- **AC5 unknown remote target/action/identity = production+destructive,
  fresh interactive TTY required, never scheduled** —
  `remote::classify_remote` + `check_remote_confirmation` unit probes.
- **AC6 remote plans bind verified SSH identity + remote state; transport
  failure explicit ambiguous/failed, no local replay** —
  `remote::remote_reply_matches`, `invoke_remote` with fake-ssh argv
  assertions (module tests: `fake_ssh_asserts_argv…`,
  `remote_state_or_identity_mismatch_is_a_typed_failure`,
  `transport_failures_are_explicit_ambiguous_or_failed`). Transport is
  argv-only `ssh` on the allowlisted PATH; no daemon RPC carries remote
  work (boundary documented in spec-lifecycle.md §6).
- **AC7 cleanup probes: dirty/uncontained work cannot be deleted;
  archived/salvaged bytes + manifests match** — symlink targets refused
  (`refusal.cleanup.symlink`), dirty refused (`refusal.cleanup.dirty`),
  `archive` preserves exact bytes + sha256 manifest into the daemon-owned
  archive root, `removed` stays false, archives never overwrite. Probes:
  `dirty_cleanup_archive_preserves_exact_bytes_and_manifest_and_never_deletes`,
  `symlinked_cleanup_targets_are_refused` (tests/mutation_engine.rs).
- **AC8 retention bounded by configured age/size; audit deletion itself an
  explicit destructive plan** — `backup::BackupPolicy` age/count/bytes
  pruning journaled as `mutate.backup.prune` inside `backup.create`
  (+ unit test `prune_backups_bounds_count_age_and_bytes`); the only
  `DELETE FROM audit` lives in the append-side retention prune; no
  purge/truncate surface (static probe
  `audit_deletion_is_append_side_retention_only`, tests/no_network_surface.rs).
- **AC9 cold-boot launchd/systemd recovery; overlap/missed ticks coalesce
  into one fresh evaluation; Herdr absent/unhealthy benign** — daemon
  serve runs one tick per due schedule after claim reconciliation with
  no external tool on PATH:
  `cold_boot_fires_due_schedule_once_per_boot_without_tools_on_path`
  (tests/daemon_lifecycle.rs) + window unit tests.
- **AC10 no network surface** — banned-token scan extended (webhook/
  websocket/mcp) over all sources incl. the new modules; daemon listener
  stays Unix-socket only (tests/no_network_surface.rs).

## Raw gate results (measured at committed head 7a1571c/c375cb4)

`just ci` aggregate (fmt-check -> check -> lint -> test -> doc ->
build-release -> security) on 2026-09-06: **exit 0** (full log
/tmp/issue9-ci.log). Individual gates, raw exit codes:

| Gate | Command | Exit |
| --- | --- | --- |
| fmt-check | `cargo fmt --check` | 0 |
| check | `cargo check --locked --all-targets` | 0 |
| lint | `cargo clippy --locked --all-targets -- -D warnings` | 0 |
| test | `cargo test --locked` (lib unit + cli_readonly, daemon_rpc, daemon_lifecycle, mutation_engine, service_plans, harness_adapters, no_network_surface) | 0 |
| doc | `cargo doc --locked --no-deps` | 0 |
| build-release | `cargo build --release --locked` | 0 |
| security | `python3 scripts/test-check-public-tree.py`; `check-public-tree.py .`; `cargo deny check`; `cargo audit`; `gitleaks dir --no-banner --redact --exit-code 1 .` | 0 |
| fixture oracle | `python3 scripts/check-contract-fixtures.py .` | 0 (65 manifest rows incl. 3 hf-schedule rows) |
| oracle self-test | `python3 scripts/test-check-contract-fixtures.py` | 0 (136 checks) |

Human gates: real remote canaries (issue #9 stop condition) were NOT run
on this lane — the remote transport is proven with fake-ssh contract
tests only (see spec-lifecycle.md §6); real launchd/systemd cold boots
were NOT run on this lane (fixture-level daemon restart probes cover the
recovery path; service-manager human gates follow the #5–#8 lane
pattern).
