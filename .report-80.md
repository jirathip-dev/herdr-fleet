# .report-80.md — explicit provider/model binding for harness prompt rows + returned-identity surfacing

Issue #80. Branch `issue-80-adapter-provider-binding`, base `origin/staging`
@ `13d59f3c537b2d9f4362c40cec73b9f928f00e3f` (verified before work).
Public repo; commit wording `Refs #80`; branch-only delivery — no PR, no
push, no GitHub mutation by this lane. Gate logs live host-local in the
lane evidence directory, outside the tracked tree, and are referred to
below by file name; only repository-relative paths appear here.

Candidate head (the commit `just ci` ran against):
`aa30e47b0667fbd0095dbdcd3fa0c9b76dfba326`. This report is committed on
top of it as a documentation-only follow-up commit (see "Gate evidence").

## What changed

- `src/adapters.rs` — `Profile` carries an explicit `provider`/`model`
  binding (`Profile::with_binding`, bare-token validated). The Pi and
  Jcode prompt rows build `--provider`/`--model` from the binding;
  `prompt_args` contains no provider/model literal. An unbound (or
  half-bound) profile refuses the terminal prompt with the documented
  typed code `refusal.binding.missing` (`CODE_BINDING`) before any
  subprocess runs — no default, no fallback, no substitution. The Jcode
  `--json` envelope now also extracts the returned `provider`/`model`;
  the typed success payload carries `requested` (the declared binding)
  and `returned` (exactly what the envelope reported, `null` when it
  reported nothing — never inferred, never coerced) alongside
  `transcript`. Non-envelope stdout still falls back to the raw
  transcript with no content loss. The Herdr lifecycle mapping reuses
  the existing typed refusal mechanism: `refusal.binding.missing` on a
  terminal `prompt` reports `blocked` with the static message
  `harness provider/model binding required`.
- `src/config.rs` — `Harness` gains optional `provider`/`model`; the
  decoder validates each declared token as a bare token (non-empty, no
  whitespace, no path separators) and requires the pair together,
  refusing with the named `config.invalid` error that spells the
  `config.harness.<key>.<field>` path. `is_bare_token` is the single
  predicate also used by the adapter-side binding constructor. The
  `config init` template comment block documents the binding pair.
- `src/schema.rs` — the `hf-config/v1` document validator accepts the
  optional `provider`/`model` keys (string type; the closed key set
  stays closed).
- `src/commands.rs` — `config show --json` surfaces the declared
  binding (`provider`/`model`, string-or-null) per harness row.
- `docs/contracts/spec-config.md` — harness row + a refusal bullet:
  bare-token pair, declared together; an absent binding is not a load
  error (the prompt refuses instead).
- `docs/contracts/spec-capabilities.md` — adds `refusal.binding.missing`
  to the closed refusal-code list, a `blocked` row to the Herdr
  lifecycle mapping table, and rewrites the harness-neutrality paragraph
  (the pair is declared input; the returned identity is surfaced; a
  requested/returned mismatch is observable and never coerced).
- `scripts/check-contract-fixtures.py` — the independent fixture oracle
  mirrors the optional binding keys (string pair; decoder-only rules
  stay decoder-only).
- `scripts/test-check-contract-fixtures.py` — two new tamper cases: a
  non-string `provider` refuses malformed; a valid `provider`/`model`
  pair is accepted.
- `tests/harness_adapters.rs` — synthetic binding tokens
  (`example-provider`/`example-model`) replace the historical literals in
  every exact-argv fake; new rows: an alternate declared pair proves the
  rows follow the binding (AC1), unbound refusal without spawning
  anything (AC3), the unbound `blocked` Herdr report (AC3), the envelope
  transcript + returned identity (AC4), requested/returned mismatch
  observable and never coerced (AC4), non-envelope raw fallback with
  `returned: null` (AC4). The prompt-path Herdr log assertions were
  hardened to the settled (last) report row: `run_op_retry` may re-run
  an op once on a transient host spawn failure, which can emit one extra
  report row (pre-fix artifact `focused-harness-adapters-2.log`, quoted
  in "Intermediate flake artifacts" below).
- Unrelated flake fixed while wiring coverage: the config unit test
  `resolve_repository_by_key_and_identity` loaded a config that named a
  policy overlay written by a different test into the shared
  per-process temp dir (order-dependent coupling under parallel test
  threads); it is now self-contained (pre-fix artifact `focused-all-1.log`,
  quoted in "Intermediate flake artifacts" below).

## Gate evidence (raw exit codes, candidate head `aa30e47`)

| Command | Raw exit | Result |
| --- | --- | --- |
| `cargo test --locked --test harness_adapters` | 0 | 44 passed; 0 failed |
| mutation: Pi prompt row literal reintroduced, same suite | 101 | 32 passed; 12 failed (RED probe bites) |
| mutation reverted (`git diff --exit-code` clean), same suite | 0 | 44 passed; 0 failed |
| `cargo test --locked` (all targets) | 0 | 12 ok targets (183 lib + 44 + 16 + 13 + …) |
| `python3 scripts/check-contract-fixtures.py .` | 0 | 65 manifest expectation(s) held (65 rows) |
| `python3 scripts/test-check-contract-fixtures.py` | 0 | 138 discrimination/coherence checks passed |
| `just ci` (contained systemd user scope, run exactly once) | 0 | fmt-check → check → lint → test → doc → build-release → security all green |

`just ci` containment facts: transient user scope with
`MemoryHigh=5G`/`MemoryMax=6G`/`MemorySwapMax=0`/`CPUWeight=50`, explicit
scope PATH and a scope-local `CARGO_TARGET_DIR`; the scope exited 0
(marker file `just-ci.scope_exit`: `scope_exit=0`). The systemd journal
for the unit was captured to `just-ci.journal.log` alongside the gate
logs; its shutdown line (host syslog prefix elided) reads:

    hf-issue80-ci.service: Consumed 2min 9.419s CPU time, 1.1G memory peak.

— well inside the 6G cap. Security leg: scanner self-test, public-tree
scan, cargo-deny, cargo-audit and the gitleaks directory scan all passed
(`no leaks found`).

## AC1 RED-probe mutation (replayable, R1 F3)

File: `src/adapters.rs`, `fn prompt_args`, the `HarnessKind::Pi` arm
(candidate-head lines 1053–1063). Canonical diff (3 lines of context
each side) — replace the binding-sourced arm with the historical literal
row:

```diff
--- a/src/adapters.rs
+++ b/src/adapters.rs
@@ -1050,17 +1050,14 @@
         // only through the allowlisted environment. The trailing `--` is
         // pi's documented end-of-options guard, so a data-last payload that
         // begins with `-` can never be parsed as an option.
-        HarnessKind::Pi => {
-            let (provider, model) = profile.prompt_binding()?;
-            Ok(vec![
-                "--provider".to_string(),
-                provider.to_string(),
-                "--model".to_string(),
-                model.to_string(),
-                "--print".to_string(),
-                "--".to_string(),
-            ])
-        }
+        HarnessKind::Pi => Ok(vec![
+            "--provider".to_string(),
+            "deepseek".to_string(),
+            "--model".to_string(),
+            "deepseek-chat".to_string(),
+            "--print".to_string(),
+            "--".to_string(),
+        ]),
         // One-shot `jcode run` row (issue #37, measured against jcode
         // v0.84.0 on 2026-09-08: the documented row exits 1 with the
         // measured missing-key text when no provider key is present, and
```

The hunk was verified against the candidate head before being recorded
here: the preimage matches the committed file byte-for-byte (a
single-occurrence block replacement in a pristine copy) and
`git apply --check --no-index` on this exact diff exits 0. The same
diff is saved host-side as `r1-red-probe-hunk.diff` in the lane evidence
directory.

Replay at the candidate head (or any head carrying that block):

1. apply the diff to `src/adapters.rs` (e.g. `git apply` from the
   worktree root);
2. `cargo test --locked --test harness_adapters` → observed raw exit 101,
   32 passed / 12 failed (`probe-leg2-mutated.log`); the 12 failing rows:
   `auth_failure_is_a_typed_credentials_refusal`,
   `hanging_prompt_is_deadline_cancelled_and_ambiguous`,
   `pi_hostile_payload_is_data_and_environment_stays_allowlisted`,
   `pi_operations_outside_herdr_never_report_lifecycle`,
   `pi_prompt_timeout_under_herdr_reports_idle_after_the_deadline_kill`,
   `pi_prompt_under_herdr_reports_idle_after_a_successful_one_shot`,
   `process_death_is_a_typed_ambiguous_outcome`,
   `prompt_rows_source_the_pair_from_the_declared_binding_not_a_literal`,
   `same_plan_fixture_passes_against_fake_implementations_of_every_adapter_contract`,
   `success_prompt_delivers_the_payload_as_data_and_returns_the_transcript`,
   `unbound_prompt_is_a_typed_refusal_without_spawning_the_harness`,
   `unbound_prompt_under_herdr_reports_blocked_with_a_static_message`;
3. reverse-apply the hunk → re-run → observed raw exit 0, 44 passed
   (`probe-leg3-revert-green.log`), and `git diff --exit-code` clean
   (raw exit 0) before the aggregate run.

## Intermediate flake artifacts (R1 F1)

Each incidental fix above was observed once as a failing run before the
fix; the decisive lines are quoted from the lane evidence directory
(host-local; not committed), with the one ephemeral host path inside the
quoted output replaced by a stable evidence identifier
(`[EV:80-prefix-config-tmpdir]` — the per-process unit-test temp dir of
that pre-fix run; nothing else in the quote changed). The
identifier-to-artifact mapping with sha256 checksums lives in
`EVIDENCE-MAP-80.md` in the lane evidence directory.

(a) `focused-harness-adapters-2.log` — the extra report row a bounded
spawn retry can emit (what `last_fake_log_line` settles):

```
---- jcode_prompt_credentials_under_herdr_reports_blocked_with_a_static_message stdout ----
thread 'jcode_prompt_credentials_under_herdr_reports_blocked_with_a_static_message' (3312043) panicked at tests/harness_adapters.rs:1825:5:
assertion `left == right` failed
  left: "pane report-agent w33:p1 --source custom:herdr-fleet-jcode --agent jcode --state idle\npane report-agent w33:p1 --source custom:herdr-fleet-jcode --agent jcode --state blocked --message harness credentials required\n"
 right: "pane report-agent w33:p1 --source custom:herdr-fleet-jcode --agent jcode --state blocked --message harness credentials required\n"
```

(b) `focused-all-1.log` — the order-dependent config-test coupling (the
test loaded a policy overlay another test writes):

```
---- config::tests::resolve_repository_by_key_and_identity stdout ----
thread 'config::tests::resolve_repository_by_key_and_identity' (3376511) panicked at src/config.rs:731:41:
load: LoadError { code: "policy.not_found", message: "cannot read policy overlay [EV:80-prefix-config-tmpdir]/policy.overlay.toml: No such file or directory (os error 2)", refusal: None, path: "[EV:80-prefix-config-tmpdir]/policy.overlay.toml" }
```

Both scenarios are pre-fix states; after the fixes every subsequent run
of the suite was green (see the gate-evidence table above).

## Not proven / human-gated

- **AC7 live canary is NOT proven here and must not be reported as
  passing.** This host has no `jcode` binary on `PATH` and no provider
  key file, so a real harness returning a real identity cannot be
  exercised. The issue records this as human-gated; the returned-identity
  surfacing is proven only against the measured envelope shape with fake
  executables.
- The real-world flag parity of the prompt rows remains
  [awaiting-evidence] per the existing adapter contract; this slice did
  not change the flag shape, only where the pair comes from.
- The aggregate was run exactly once per the brief's containment rule; it
  exercises the candidate head above — the documentation-only report
  commit was not re-gated (per the brief's single-run rule).
