# .report-106.md — product rename `herdr-fleet` → `canter` with pre-rename compatibility

Issue #106 (complete the public product rename with hard compatibility and
state-path safety). Branch `issue-106-canter-rename`, base `origin/staging` @
`690eea081b62eedc3b38400c64803ba2db22dbaa`. Feature head
`bfd5f76674339d188b3c51686e0782afa8a8c749` — every gate, probe, and focused
check below was measured against exactly that tracked tree; the delivered head
is the commit that carries this report on top of it, and the pushed branch sha
is reported in the lane handoff. Commit wording `Refs #106`; **no** PR, issue,
or other GitHub mutation was performed by this lane, and the only remote ref
touched is this branch. No host paths, private repository names, credentials,
provider/model policy, or scheduler identity appear in this report or in the
diff.

## 1. What changed

- **Package / crate / binaries**: `Cargo.toml` package + library crate renamed
  to `canter` (description and repository URL updated); canonical binary
  `canter`; the pre-rename `herdr-fleet` name ships as a second thin bin
  target (`src/bin/herdr-fleet.rs`) that prints a one-line deprecation warning
  on stderr and runs the identical CLI through the shared
  `commands::cli_main` (the CLI entry moved from `src/main.rs` into the
  library so both binaries execute one code path).
- **Identity and docs surface**: `--help`/`--version` (package name, about
  line, and the unchanged schema facts: state schema version 7, migration
  chain m0001–m0007, 17 `hf-*` document families), README, runbook
  (`docs/OPERATIONS.md`), `docs/ARCHITECTURE.md`, ADR-0001/0003, the
  `docs/contracts/` corpus, the public skill (directory renamed
  `skills/herdr-fleet` → `skills/canter`), `docs/DEVELOPMENT.md`,
  `docs/RELEASING.md`, `docs/WORKFLOW.md`, `AGENTS.md`, `SECURITY.md`,
  `CONTRIBUTING.md`, `LICENSE-MIT`, issue/PR templates, `CODEOWNERS`,
  Dependabot config, `justfile`/`deny.toml` comments, and `CHANGELOG.md`
  (new rename entry; earlier entries kept as history).
- **Build/release surface**: `scripts/build-archive.py` (archive name
  `canter-<version>-<platform>.tar.gz`, payload binary member `canter`, SBOM
  root package `canter`, provenance `product: canter`, `--version` line
  matcher), `scripts/test-build-archive.py`, `scripts/clean-host-verify.sh`
  (version-line match), `scripts/clean-host-probe.py`, `measure-baseline`
  helpers, and a `test-check-public-tree.py` fixture URL.
- **CI**: `ci.yml` macOS smoke now asserts the canonical binary and the alias
  binary (`target/debug/herdr-fleet --version` must print the `canter`
  identity and warn on stderr); required-files list follows the renamed skill
  path; header comments updated on both workflows.
- **Tests**: crate-path / binary-name renames across the integration suites,
  plus two new machine checks — `tests/rename_compat.rs` (pre-rename
  behavior over the real binaries) and `tests/rename_sweep.rs` (stale
  reference sweep with pinned legacy counts).

Deliberately **not** renamed (live/external identity, or dated evidence — the
single normative list is `docs/contracts/compatibility.md`, "Product rename
(issue #106)"):

- Herdr custom-integration source ids `custom:herdr-fleet-pi` /
  `custom:herdr-fleet-jcode` (`src/adapters.rs`) and the default Herdr /
  terminal session name `herdr-fleet-lane` (`src/mutation.rs`): live
  registry / lane identity, explicitly outside a product-surface rename.
- Frozen v0.1.0 / locked-target architecture render sets
  (`docs/architecture/herdr-fleet.*`; filenames, rendered titles, and pinned
  SHA-256 kept byte-frozen) and historical `.report-*.md` records.
- Machine contract: `hf-*` schema families, envelopes, exit codes, daemon
  RPC; upstream `herdr` (the tool) references throughout.

## 2. Compatibility + state continuity (hard requirement)

Normative contract (single home, linked from README / CHANGELOG / OPERATIONS /
spec-config / skill): `docs/contracts/compatibility.md`, section "Product
rename (issue #106)". Delivered behavior:

1. A **pre-rename state/runtime tree** (`$XDG_STATE_HOME/herdr-fleet/`,
   `$XDG_RUNTIME_DIR/herdr-fleet/`) is **adopted in place** when it exists and
   the `canter/` tree does not: derivation keeps socket and state together,
   nothing is copied, moved, migrated, or deleted, and the daemon opens and
   migrates the existing SQLite state normally. Once a `canter/` tree exists
   it is authoritative; a leftover pre-rename tree is ignored and never
   touched (`src/dirs.rs`: `DIR_NAME` / `LEGACY_DIR_NAME` /
   `adopted_dir_name`).
2. A **pre-rename config** (`$XDG_CONFIG_HOME/herdr-fleet/config.toml`) is
   still discovered when no `canter/config.toml` exists and is read in place
   (`src/config.rs`: `discover_config` / `legacy_config_path`); the new path
   wins when both exist.
3. The **pre-rename binary name** keeps working (alias binary, §1), and the
   **pre-rename crash-point env var** `HERDR_FLEET_CRASH_POINT` is still
   honored next to `CANTER_CRASH_POINT` (debug builds only;
   `src/daemon.rs` `crash_point_requested`, unit-tested for preference
   order).
4. Pre-rename **service units** keep working (their program path is the
   installed binary and the alias above resolves it); new `service *-plan`
   output renders `com.canter.daemon` / `canter.service`. The CLI never
   touches the service manager.

Proof (raw exits; logs under `run/logs/`, lane-local and git-excluded):

| Proof | Command (from the repo root) | Raw exit |
| --- | --- | --- |
| alias binary = same CLI (+ stderr warning) | `cargo test --locked --test rename_compat pre_rename_alias_binary_runs_the_identical_cli` | `0` (`run/logs/ev-compat-alias.log`) |
| pre-rename tree adopted in place, reused by a second process, marker bytes untouched, no `canter/` tree created | `cargo test --locked --test rename_compat pre_rename_state_tree_is_adopted_in_place_and_reused` | `0` (`run/logs/ev-compat-state.log`) |
| pre-rename config discovered; new path wins when both exist | `cargo test --locked --test rename_compat pre_rename_config_path_is_discovered_and_the_new_path_wins` | `0` (`run/logs/ev-compat-config.log`) |
| dirs derivation rules (fresh / adopt / new-wins) | `cargo test --locked --lib dirs::tests::` | `0` (`run/logs/ev-dirs-unit.log`) |
| crash-point env alias preference | `cargo test --locked --lib daemon::tests::crash_point_env_alias_prefers_canonical_and_honors_legacy` | `0` (`run/logs/ev-env-alias.log`) |
| CLI smoke (canonical binary) | `cargo test --locked --test cli_smoke` | `0` (`run/logs/ev-smoke.log`) |

The state proof is end-to-end: an empty pre-rename state directory plus a
marker file is prepared before the first run; run 1 opens/migrates state **in
that tree** (asserted: `state.db` and `daemon.log` under `herdr-fleet/`, no
`canter/` created, `schema_version` = 7 via a live `status` RPC on the
pre-rename-named socket), then a second fresh process reuses the same tree
and the same state epoch. The marker file is asserted byte-identical across
both phases (no destructive migration).

## 3. Repo-wide sweep

    cargo test --locked --test rename_sweep   # raw exit 0 (run/logs/ev-sweep.log)

The sweep scans `git ls-files` (the tracked tree only) for `herdr-fleet`,
`herdr_fleet`, and `HERDR_FLEET`; `.report-*`, `docs/architecture/herdr-fleet.*`
(historical / dated records, the latter also inside the compatibility
contract), and the sweep test itself are excluded by design. Every remaining
file must match a **pinned** allowlist entry (occurrence count + reason inline
in `tests/rename_sweep.rs`). Result: **zero stale references** outside the
pinned legacy set (22 allowlisted files: the alias source, the compatibility
contract, live-id carriers, frozen-artifact references, and the compat tests
themselves). Adding or removing a deliberate legacy mention requires updating
the pin and the contract together.

## 4. Gates (exact commands, raw exits)

`just --list` was run first (exit 0; recipes: `fmt`, `fmt-check`, `check`,
`lint`, `test`, `doc`, `build-release`, `security`, `ci`, `default`).

Logs are lane-local under `run/logs/` (git-excluded); every exit below is the
raw exit of the named command.

| Gate | Raw exit | Log |
| --- | --- | --- |
| `just ci` (aggregate at feature head `bfd5f76`) | `0` | `run/logs/ci-106-head1b.log` |
| `just fmt-check` | `0` | `run/logs/recipe-106-fmt-check.log` |
| `just check` | `0` | `run/logs/recipe-106-check.log` |
| `just lint` | `0` | `run/logs/recipe-106-lint.log` |
| `just test` | `0` | `run/logs/recipe-106-test.log` |
| `just doc` | `0` | `run/logs/recipe-106-doc.log` |
| `just build-release` | `0` | `run/logs/recipe-106-build-release.log` |
| `just security` | `0` | `run/logs/recipe-106-security.log` |
| `python3 scripts/test-build-archive.py` | `0` | `run/logs/script-test-build-archive-rerun2.log` |
| `python3 scripts/check-contract-fixtures.py` | `0` | `run/logs/script-check-contract-fixtures.log` |
| `python3 scripts/test-check-public-tree.py` | `0` | `run/logs/script-test-check-public-tree.log` |

Notes (disclosed, not silently smoothed over):

- `just security` requires gitleaks — present (8.30.1) and it reported "no
  leaks found"; cargo-deny reported advisories/bans/licenses/sources ok;
  cargo-audit loaded the advisory database and passed.
- One first-pass artifact: during the first `just ci` pass the lockfile was
  rewritten into canonical alphabetical package order (the renamed root
  entry had stayed in its old position); the only non-`--locked` cargo
  invocations in the chain are the security recipe's `cargo deny`/`cargo
  audit` steps. The sorted lockfile was committed and everything re-run; the
  first `test-build-archive.py` attempt in that window exited 1 on the
  builder's own "clean tracked worktree" guard (dirty `Cargo.lock`) — not a
  product defect; the re-run above is green at the feature head.
- `python3 scripts/test-build-archive.py` was run with the lane's
  `CARGO_TARGET_DIR` override *unset*: the script resolves the release binary
  at `<repo>/target/release/canter` by its own documented contract, so with
  the override it would look in the wrong directory. All cargo invocations
  inside it are still `--locked`.
- Isolation for everything else: `CARGO_TARGET_DIR=<worktree>/target-106`,
  `CARGO_BUILD_JOBS=4`, `--locked` everywhere.

## 5. Mutation probes (each RED, then byte-restored and proven clean)

| # | Mutation | RED (raw exit / log) | Restore proof | GREEN (raw exit / log) |
| --- | --- | --- | --- | --- |
| A | alias binary stops delegating to the CLI | `101` — `run/logs/probe-a-red.log` (`--version through the alias must exit 0`) | `cmp` byte-identical + `git diff` clean | `0` — `run/logs/probe-a-green.log` |
| B | state adoption removed (always `canter/`) | `101` — `run/logs/probe-b-red.log` (daemon never serves the pre-rename socket) | `cmp` byte-identical + `git diff` clean | `0` — `run/logs/probe-b-green.log` |
| C | stale `herdr-fleet` reference reintroduced in a machine-checked file (`AGENTS.md`) | `101` — `run/logs/probe-c-red.log` (`AGENTS.md: 1 stale pre-rename needle(s) outside the documented legacy/historical set`) | `cmp` byte-identical + `git diff` clean | `0` — `run/logs/probe-c-green.log` |

All RED failures are real test-assertion failures (not compile errors). After
each restore the file is `touch`ed before the GREEN run so the compiler
cannot reuse a mutated artifact, and each GREEN run re-executes the same
focused check; `git diff` on the restored path is empty.

## 6. Commit / branch / push

- Feature commit (code, docs, scripts, CI, tests): `bfd5f76` — message
  `Rename the product herdr-fleet -> canter with in-place compatibility (Refs #106)`.
- This report is the only additional tracked change on top of it.
- Branch pushed to the renamed public repository (`jirathip-dev/canter`);
  it did not exist before this lane (no force-push). Remote readback of
  `refs/heads/issue-106-canter-rename` and the delivered head sha are in the
  lane handoff message.

## 7. Not satisfied / disclosed gaps

- **Frozen architecture renders** keep the v0.1.0-era name inside their
  HTML/JSON/PNG content and their filenames by design (dated, SHA-256-pinned
  records; regeneration is a separate rendering workstream with recorded
  byte-reproducibility caveats). Same for historical `.report-*.md` records.
- **A released pre-rename binary** was not run against this tree: no release
  exists, so the state-continuity proof uses the delivered binary against a
  pre-created pre-rename tree and the normal migration path rather than a
  byte-identical copy of a shipped v0.1.0 binary.
- **Live Herdr registry/session identifiers** keep their pre-rename spelling
  by fence, not by preference: this lane is not authorized to rename live
  registry identity, and the alias/fallbacks are removed only by a future
  release that announces it in CHANGELOG.md.
- **Hosted CI** runs (the `ci.yml` alias-smoke step) can only be exercised by
  the orchestrator's PR; the local equivalent was executed here
  (`cargo test --locked` + the macOS smoke's commands).

## 8. Review-fix round #106-R1 (PR #108) — the release archive ships the pre-rename alias

Adjudicated blocking finding (the only item): `docs/contracts/compatibility.md`
(and `CHANGELOG.md`) promise the legacy `herdr-fleet` alias binary is **built
and shipped** next to `canter`, but the archive built at the reviewed head
`b6fea798843eab5ae159d9180030a3cdcf1e0bbc` contained only `canter` plus
metadata/licences — the shipped artifact contradicted the authoritative
compatibility contract. Fix head
`11f4c9d76505f7fa817e06d22fed218d10c2fec4`; this section is the report commit
on top of it.

### 8.1 The member name (stated verbatim, per the fix contract)

The archive member is **`herdr-fleet`**, at `canter-<version>/herdr-fleet` —
literally next to `canter-<version>/canter`, matching the contract wording
("built and shipped next to `canter`"). The builder was corrected to match the
contract; the contract sentences were neither weakened nor deleted.

### 8.2 What changed (fix head `11f4c9d`, diffstat `+94/-18` over `b6fea79`)

- `scripts/build-archive.py` — `LEGACY_ALIAS = "herdr-fleet"`;
  `ARCHIVE_MEMBERS` lists it; `build` requires an executable alias next to
  `--binary` (the same `cargo build --release --locked` produces it), copies
  it into the version directory, gives it the 0o755 executable framing, and
  adds it to the per-file manifest so `SHA256SUMS` and `provenance.files` both
  carry its digest.
- `scripts/test-build-archive.py` — new witnesses: the archive must contain
  the alias member (member set compared against the builder's own
  `ARCHIVE_MEMBERS`), the member must be mode `755`, and its digest must match
  both `SHA256SUMS` and `provenance.files`. The crafted-traversal fixture now
  derives its benign member set from the builder's member list, so the
  `extractall` refusal remains the guard actually under test.
- `tests/rename_sweep.rs` — two pins added (`scripts/build-archive.py`: `1`;
  `scripts/test-build-archive.py`: `1`). The alias member name is a deliberate
  legacy mention in the two changed scripts; the sweep's own doc comment
  requires such mentions to be pinned in its enumerated legacy set, and
  without the pins `cargo test` goes red. This is the fix contract's
  "dependent ... membership test" and the only file touched beyond the two
  named scripts.

### 8.3 Evidence at the fix head (raw exits + named logs)

`just --list` was run first (exit `0`). Logs are lane-local under `run/logs/`
(git-excluded); every exit below is the raw exit of the named command.

| Command | Raw exit | Log |
| --- | --- | --- |
| `cargo build --release --locked` | `0` | `run/logs/fix1-cargo-build.log` |
| `python3 scripts/test-build-archive.py` | `0` | `run/logs/fix1-green-selftest.log` |
| `python3 scripts/build-archive.py build --repo "$PWD" --source-ref 11f4c9d... --version 0.1.0 --binary "$PWD/target/release/canter" --platform darwin-aarch64 --out-dir run/fix1-archives` | `0` | `run/logs/fix1-archive-build.log` |
| `tar -tzf canter-0.1.0-darwin-aarch64.tar.gz` | `0` | `run/logs/fix1-archive-list.log` |
| `python3 scripts/build-archive.py verify --archive <archive>` | `0` (6 checks) | `run/logs/fix1-archive-verify.log` |
| `shasum -a 256 -c canter-0.1.0-darwin-aarch64.tar.gz.sha256` | `0` | `run/logs/fix1-archive-shasum.log` |
| extracted `canter-0.1.0/herdr-fleet --version` | `0` | `run/logs/fix1-archive-alias-run.log` |
| `just ci` (aggregate incl. `cargo test` with the new sweep pins) | `0` | `run/logs/fix1-ci.log` |

Archive member listing (`canter-0.1.0-darwin-aarch64.tar.gz`):

    canter-0.1.0/LICENSE-APACHE
    canter-0.1.0/LICENSE-MIT
    canter-0.1.0/SBOM.spdx.json
    canter-0.1.0/SHA256SUMS
    canter-0.1.0/canter
    canter-0.1.0/herdr-fleet
    canter-0.1.0/provenance.json

Archive sha256: `393029391aed1d6d8dc775177ad9812d2ccfdecfb806e86e9009801635a029cf`;
`verify` reports `6 checks passed`; the adjacent `.sha256` check is `OK`; the
archived alias runs the identical CLI (deprecation warning on stderr,
`canter 0.1.0` + schema facts on stdout, exit `0`).

### 8.4 RED evidence at the reviewed head (throwaway probe worktree)

- Reviewer repro re-run at `b6fea79` with the pre-fix builder: build RAW exit
  `0`; the member listing (`run/logs/fix1-red-prefix-archive-list.log`)
  contains **no alias member** — the finding reproduces.
- The updated self-test against the pre-fix builder: RAW exit `1`,
  `FAIL: ['LICENSE-APACHE', 'LICENSE-MIT', 'SBOM.spdx.json', 'SHA256SUMS',
  'canter', 'provenance.json']` (`run/logs/fix1-red-selftest.log`): the new
  witness is born red before the fix and green after.

### 8.5 Mutation battery (probe worktree; self-test run per mutation)

Control (unmutated fix head in the same probe harness): RAW exit `0`
(`run/logs/fix1-probe-control-selftest.log`).

| # | Mutation | Raw exit | What failed (log) |
| --- | --- | --- | --- |
| M1 | alias removed from `ARCHIVE_MEMBERS` | `1` | verify layout mismatch — expected without `herdr-fleet`, found with it (`fix1-probe-m1-selftest.log`) |
| M2 | `build` no longer copies the alias | `1` | builder fails closed on the inconsistent member set (manifest-step `FileNotFoundError`); self-test red (`fix1-probe-m2-selftest.log`) |
| M3 | alias dropped from the manifest only | `1` | verify passes its 6 checks; the **new witness** fails (`FAIL: {...sums without herdr-fleet...}`) (`fix1-probe-m3-selftest.log`) |
| M4 | alias loses executable framing (mode 644) | `1` | verify passes; the **new witness** fails (`FAIL: 0o644`) (`fix1-probe-m4-selftest.log`) |

No mutation stayed green; M3/M4 are exactly the wiring/framing gaps `verify`
alone cannot see.

### 8.6 Scope decisions / disclosed

- `docs/RELEASING.md`'s layout block (a copy of the builder's member listing)
  still shows only `canter` plus metadata. The fix contract scoped the change
  to the builder and its self-test ("do not touch anything else"; "fix the
  BUILDER to match the contract") and neither promises nor permits other doc
  edits, so it was left as written; flagged here for the orchestrator to
  decide on a possible follow-up. `docs/contracts/compatibility.md` and
  `CHANGELOG.md` were already correct and were not touched.
- The probe worktree and scratch outputs are removed at closeout; lane logs
  stay under `run/logs/` (git-excluded).
- Closeout `git status --porcelain` shows only the lane-local untracked
  `.fix-r1.md` (the adjudicated fix contract handed to this lane; the exact
  output is in the lane handoff message).
