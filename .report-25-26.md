# Round report — docs-25-26 (issues #25 + #26, one docs-only lane)

Branch `issue-25-26-docs`, based on `origin/staging @ 3a399e60658bcf18910235f39997c55f4977434a`
(verified `git rev-parse HEAD` before starting). DOCS ONLY: no changes under
`src/`, `tests/`, `justfile`, `.github/`, `Cargo.*`, `schemas/`, or
`scripts/`. No version bump. Commits use `Refs #25` / `Refs #26` wording
(never Fixes/Closes/Resolves); the PR body closes the issues.

All exercised outputs were captured from the real release binary
(`cargo build --release --locked`) in a private disposable sandbox with
synthetic fixtures only: fake `gh`/`herdr` executables on a controlled
PATH, a synthetic local git checkout of `example-org/widgets`, and isolated
XDG config/state/runtime directories. Nothing real was mutated. Quoted
output is trimmed; host paths, pids, and timestamps are redacted
(`.../`, `<pid>`, `<ts>`).

## AC map

### Issue #25 (README drift + CHANGELOG)

| AC | Delivered in |
| --- | --- |
| Command table matches `--help` on the release binary exactly (all 13 shipped subcommands, one line each) | `README.md` "Command surface" — mirrored from `herdr-fleet --help` (run in this worktree; exit 0); status/boundaries rewritten to plan-first, daemon-mediated, grant-gated truth without production overclaims. |
| Quickstart: ≥1 example per stable surface family (observe, plan, daemon, service), each exercised | `README.md` "Quickstart": doctor+status (observe), plan (offline `--revision`), daemon run + daemon status, service install-plan — all run against the binary; outputs trimmed/redacted and labeled as such. |
| CHANGELOG Added/Changed for #3-#12 under [Unreleased], naming issue/PR | `CHANGELOG.md` — one "Added (issue #N — title, PR #M)" subsection per stage #3-#10 + #12 Changed; issue titles fetched from GitHub, PR numbers verified against repo history (PR #14..#21, #13). |
| Docs-index links verified non-dead from a clean checkout | Doc-link audit (see below) — README + all docs/**.md resolved from a clean worktree at HEAD; directory links fail closed. |
| docs: updated claims re-verified by re-reading the rendered pages | Every README/OPERATIONS/SKILL claim was re-read against the real `--help`/`--version` and exercised command output; no claim rests on file presence alone. |

### Issue #26 (OPERATIONS.md + SKILL.md refresh)

| AC | Delivered in |
| --- | --- |
| OPERATIONS.md human runbook, task-ordered, every claim exercised | `docs/OPERATIONS.md` — install → configure → observe → plan → daemon/service lifecycle → grant-gated mutations (dry-run render → grant → apply → verify per #8 semantics) → cleanup/archives → crash/cold-boot recovery → remote transport boundary → upgrade; each section: prerequisites, exact commands, expected envelope/exit-code shapes, failure→remedy tables; section 6 explicitly synthetic (no real mutation anywhere). |
| Skill covers 100% of `--help` subcommands, each marked read-only or grant-gated | `skills/herdr-fleet/SKILL.md` — command-surface table with all 13 shipped commands classified read-only / local-state-server / grant-gated (daemon `apply` RPC, not a CLI subcommand). |
| Fresh-agent canary: agent with only the skill + binary completes observe→plan unaided | `skills/herdr-fleet/SKILL.md` "Observe → plan (read-only) flow" — numbered 5-step flow with expected exit codes/envelopes; the exact sequence was run against the binary in this round (see exercised table, rows 4-15). A literal fresh-agent run is an orchestrator/reviewer step (no agent spawns from this lane). |
| docs updated in the same PR; links non-dead | README docs index now links `docs/OPERATIONS.md`; doc-link audit below (README, docs/, skills/). |
| No code changes beyond docs/skill files | Diff = README.md, CHANGELOG.md, docs/OPERATIONS.md (new), skills/herdr-fleet/SKILL.md, .report-25-26.md (new). |

## Exercised-command table (raw exit; truthful redacted quote)

Sandbox: release binary; fake `gh` (reports version/auth scopes, answers
`repos/example-org/widgets` and `.../issues/7`), fake `herdr` (`--version`
→ `herdr 0.8.2`); config = fixture-style `hf-config/v1` with
`example-org/widgets`; synthetic local checkout with matching origin;
isolated XDG dirs. PATH tool presence was varied per row.

| # | Command | Exit | Redacted output quote / note |
| --- | --- | --- | --- |
| 1 | `herdr-fleet --help` | 0 | USAGE lists `config init`, `config validate [--config PATH] [--json]`, `config show ...`, `doctor [--json]`, `status ...`, `plan <repository> <issue> [--revision HEX40] ...`, `capabilities [--json]`, `daemon run [--socket PATH] [--config PATH]`, `daemon status ...`, `service doctor|install-plan|status-plan|uninstall-plan ...`; "EXIT CODES ... 0 ok · 1 operational error · 2 usage · 3 partial · 4 refusal · 5 config error". |
| 2 | `herdr-fleet --version` | 0 | `herdr-fleet 0.1.0`; `state schema version: 4`; `migration chain: m0001_initial_state_v1, m0002_workflow_engine_instances_v2, m0003_control_plane_evidence_v3, m0004_schedules_lifecycle_v4`; 17 `document schema families` incl. `hf-config/v1 ... hf-outcome/v1, hf-workflow/v1, hf-schedule/v1`. |
| 3 | `herdr-fleet config init` | 0 | Prints annotated `hf-config/v1` TOML template (schema line + commented daemon/policy/repository/harness/workflow tables) to stdout; writes nothing. |
| 4 | `herdr-fleet config validate --json` (valid) | 0 | `{"command":"config validate","data":{...,"valid":true},"exit_code":0,"kind":"ok","schema":"hf-output/v1"}` |
| 5 | `config validate --json` (schema `hf-config/v2`) | 5 | `{"command":"config validate","error":{"code":"config.invalid","details":{"refusal":"refuse-version"},"message":"unsupported version \"hf-config/v2\" for hf-config",...},"exit_code":5,"kind":"error",...}` |
| 6 | `herdr-fleet config show --json` | 0 | Data shows `daemon_enabled:true`, repository `example-org/widgets` (key `widgets`, branch `staging`, origin `...github.com/example-org/widgets`), harness `cli` (argv), policy overlay valid. |
| 7 | `doctor --json` (fake tools ok) | 0 | checks `git`/`herdr`/`gh` all `status:"ok"`; `gh 9.9.9; authenticated; scopes: repo, read:org`; config row ok. |
| 8 | `doctor --json` (empty PATH) | 3 | kind `partial`; each check `status:"missing"` with cause text (`git not found on PATH ...`, `herdr not found on PATH ...`, `gh not found on PATH ...`). |
| 9 | `doctor --json` (invalid config) | 5 | `{"error":{"code":"config.invalid",...},"exit_code":5,...}` (refusal `refuse-version`). |
| 10 | `capabilities --json` | 0 | `{"capability":{"actor":"herdr-fleet","axis":"forge","capabilities":["read_refs","read_issues","read_checks"],"schema":"hf-capability/v1"},...}` |
| 11 | `status --json` (in checkout, fake gh) | 0 | kind ok; `freshness:"fresh"`, `observed_repositories:1`; observations complete: herdr present 0.8.2; git `origin_matches:true`, head 40-hex; github `identity_matches:true`. |
| 12 | `status --json` (no gh on PATH) | 0 | kind ok; `github:{"available":false,"reason":"gh.unavailable",...}` — explicit degradation, freshness stays fresh. |
| 13 | `status --json` (outside a checkout) | 0 | `git:{"available":false,"reason":"no_local_checkout",...}` while github probe completes. |
| 14 | `plan example-org/widgets 7 --revision 0123...4567 --json` ×2 | 0 / 0 | Deterministic: two runs byte-identical (`cmp` clean). `digest: 8164...e87b`, `issue_source:"argument"`, plan: `schema hf-plan/v1`, `state_epoch:0`, 8 doctrine steps p1..p8, `workflow_id:"fleet-doctrine-1"`, `workflow_hash` 64-hex. |
| 15 | `plan example-org/widgets 7 --json` (fake gh answers issue) | 0 | `issue_source:"github"`, revision = 40-hex digest of the redacted acceptance text; plan envelope identical shape to row 14. |
| 16 | `plan ... 7 --json` (no gh, no --revision) | 1 | `{"error":{"code":"forge.unavailable",...},"message":"gh unavailable: ...; pass --revision <40hex> to render the plan offline",...}` |
| 17 | `plan not-configured 1` | 2 | stdout empty; stderr: `plan: no configured repository matches "not-configured"; configure it first (see 'herdr-fleet config init'); run 'herdr-fleet plan --help'`. |
| 18 | `daemon run` (isolated XDG; foreground) | n/a | daemon.log: `{"event":"daemon.ready",...,"message":"state open; reconciled 0 interrupted claim(s); serving .../herdr-fleet/daemon.sock",...}`; stopped with SIGTERM. |
| 19 | `daemon status --json` (live) | 0 | `{"command":"daemon status","data":{"daemon":{"pid":<pid>,"started_at":<ts>,"version":"0.1.0"},"freshness":"fresh","state":{"active_grants":0,"epoch":1,"event_seq":0,"journal_seq":0,"pending_claims":0,"poisoned":false,"schema_version":4}},...}` |
| 20 | second `daemon run` while live | 1 | stderr: `another daemon is already running: another daemon holds the lock (pid <pid> (started <ts>))`. |
| 21 | `daemon status --json` after SIGTERM | 1 | `{"error":{"code":"daemon.stale",...,"retryable":true},"message":"a stale daemon socket exists at .../herdr-fleet/daemon.sock; a fresh \`daemon run\` reclaims it",...}` |
| 22 | fresh `daemon run` (reclaim) + `daemon status --json` | 0 | live again (same envelope shape as row 19) — stale socket reclaimed. |
| 23 | `daemon status --json` after `kill -9` | 1 | `daemon.stale` (retryable) as row 21. |
| 24 | fresh `daemon run` after crash + status | 0 | cold-boot reclaim: ready log + live status ok. |
| 25 | `service doctor --json` | 0 | checks: platform `systemd` ok, config `daemon.enabled=true` found, socket `absent` (daemon not running), service-unit `absent`; summary `{"daemon":"absent","platform":"systemd","unit":"absent"}`. |
| 26 | `service install-plan --json` | 0 | `platform:"systemd"`; steps: write rendered unit to `.../.config/systemd/user/herdr-fleet.service`, `systemctl --user daemon-reload`, `systemctl --user enable --now herdr-fleet.service`, verify `systemctl --user --no-pager status herdr-fleet.service`; `unit` contains `ExecStart=<herdr-fleet-binary> daemon run --socket .../herdr-fleet/daemon.sock` (`Restart=on-failure`, `NoNewPrivileges=true`); never activates. |
| 27 | `service status-plan --json` | 0 | steps: `systemctl --user --no-pager status herdr-fleet.service`; `herdr-fleet daemon status --json`; same unit text. |
| 28 | `service uninstall-plan --json` | 0 | steps: `systemctl --user --no-pager disable --now herdr-fleet.service`; `rm -f .../.config/systemd/user/herdr-fleet.service`; `systemctl --user daemon-reload`; same unit text. |

Note: quoted binary paths in service-plan output are rendered by the binary
from its argv0 + config; redacted above as `<herdr-fleet-binary>` and
`.../`.

## SKILL coverage checklist (100% of `--help`)

| `--help` subcommand | Classification in SKILL.md |
| --- | --- |
| `config init` | read-only |
| `config validate` | read-only |
| `config show` | read-only |
| `doctor` | read-only |
| `status` | read-only |
| `plan` | read-only (renders; NEVER applies) |
| `capabilities` | read-only |
| `daemon run` | local state server (no fleet/external mutation) |
| `daemon status` | read-only probe |
| `service doctor` | read-only |
| `service install-plan` | read-only (renders; never activates) |
| `service status-plan` | read-only (renders) |
| `service uninstall-plan` | read-only (renders) |
| daemon `apply` RPC (not a CLI subcommand) | **grant-gated mutation** |

## Gate table

Run at the final committed head (tools: cargo 1.97.1, just 1.46.0, gitleaks
8.30.1, python3). Raw exit codes captured; logs named below. Results are
recorded in the report's final amendment commit (this file is updated by
the lane after the runs; the amendment commit is part of this branch).

| Recipe | Raw exit | Log |
| --- | --- | --- |
| `cargo build --release --locked` | 0 | /tmp/issue25-26-build.log |
| `just fmt-check` | 0 | /tmp/issue25-26-fmt.log |
| `just check` | 0 | /tmp/issue25-26-check.log |
| `just lint` | 0 | /tmp/issue25-26-lint.log |
| `just test` | 0 | /tmp/issue25-26-test.log |
| `just security` | 0 | /tmp/issue25-26-security.log |
| `just ci` (FINAL aggregate) | recorded in final commit | /tmp/issue25-26-just-ci.log |

`just --list` (canonical recipes): fmt, fmt-check, check, lint, test, doc,
build-release, security, ci.

Note on `just test`: an initial run under a polluted shell environment
(sandbox `HOME`/`XDG_CONFIG_HOME` exports leaking into the process env)
failed one env-dependent unit test (`commands::missing_config_is_a_typed_config_error`,
expected exit 5 for an absent config, saw 1 because the exported HOME
contained a valid config). Re-run with a clean environment: 177 lib tests +
15 cli_readonly + 6 cli_smoke + 5 daemon_lifecycle + 13 daemon_rpc + 17
harness_adapters + 11 mutation_engine + 3 no_network_surface + 3
service_plans + doc-tests — all ok, exit 0. No code changed; not a
regression.

## Doc-link audit

From a clean checkout at HEAD (`git worktree add` of this commit in a temp
dir), every relative markdown link under `README.md`, `docs/` (hosted
policy resolution: file-dir relative), and `skills/herdr-fleet/SKILL.md`
(repo-root relative, matching the skill's existing convention) resolves to
an existing FILE; directory links fail. Result: 0 broken links (details in
the audit output; see the lane closeout evidence).

## AGENTS.md disposition — DEFERRED (protected instruction file)

AGENTS.md is a protected instruction file and was NOT edited. It is,
however, stale on one claim and warrants a follow-up truthfulness update
outside this docs round: the "Repository contract" preamble still says the
repository is a read-only bootstrap where "No daemon, workflow engine,
adapter, mutation, migration, or release behavior exists or may be added
here until its roadmap slice is routed (umbrella issue #1; children #3-#10
are unrouted)". Children #3-#10 (+ polish #12) are implemented on staging
(PRs #13-#21) and this round's docs describe the shipped surface.
Intended replacement (for the owning process/round): reword the preamble to
state the repository now ships the plan-first surface from #3-#10 on
staging (read-only CLI core, single-writer daemon, workflow engine, harness
adapters, daemon-mediated grant-gated mutations, lifecycle + release-
readiness machinery) while `main` remains stable/release and live workflow
execution, real harness parity, and release execution stay human-gated.
README.md (updated here) is the authoritative current-surface statement
until that edit is routed.

## What could NOT be verified in this docs lane

- A literal fresh-agent canary run (an agent spawned with only the skill +
  binary) — no agent spawns from this lane; the skill's 5-step flow was
  executed manually with the binary (rows 4-15) and is ready for reviewer
  reproduction.
- Hosted CI policy checks at the PR head (policy job incl. doc-link check,
  rust-ubuntu/macos, supply-chain, secret-scan) — they run on the PR the
  orchestrator opens; the local aggregate mirrors them (gate table above).
- Cross-platform (macOS/launchd) service-plan rendering — exercised on
  Linux/systemd only; the launchd branch is covered by the macOS CI job and
  `tests/service_plans.rs`.
- The `apply` RPC flow end to end (grant issue → apply → typed outcome) —
  RPC-only surface, exercised by `tests/mutation_engine.rs` /
  `tests/daemon_rpc.rs` (unchanged, green under `just test`); OPERATIONS.md
  section 6 is explicitly synthetic contract guidance, no real mutation was
  or can be performed from this repo.
