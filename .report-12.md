# Report — issue #12 post-bootstrap polish (docs-only)

Branch: issue-12-polish (base origin/staging b6234cc)
Scope: docs-only micro-polish — no code, gate, or behavior changes.
Refs #12; bootstrap issue #2 stays open (no Fixes/Closes used).

## Finding 1 — justfile `security` recipe doc-comment (fixed)

The recipe's doc comment spanned three lines. `just --list` renders the
last doc-comment line, so it printed the continuation text instead of the
summary. Reduced the comment to the single one-line summary, matching the
one-line doc-comment convention of every other recipe; the removed
explanation (gitleaks required / fails with an actionable message) remains
authoritative in the canonical developer docs and in the recipe body
itself.

Before (`just --list`):
    security      # actionable message instead of silently skipping the content scan.

After (`just --list`):
    security      # Public-tree/privacy scanner (self-test + scan) + supply-chain checks.

## Finding 2 — dark-preview renderer note (added)

docs/architecture/README.md: added a short note under the "Regeneration"
section stating that the light preview re-renders byte-exactly, the dark
preview is NOT byte-reproducible on re-render with the current local
Chromium / chrome-headless-shell (antialiasing-level raster noise only;
geometry/content identical to the committed PNG), and the exact renderer
binary must be pinned there if dark byte-reproducibility is ever required.

## Verification

Full local gate run on this branch head:
    just ci  (fmt-check -> check -> lint -> test -> doc -> build-release -> security)

- scanner self-test: 21 tests, OK
- public-tree scan: passed
- cargo-deny: ok (advisories/bans/licenses/sources)
- cargo-audit: no vulnerabilities (1 crate dependency scanned)
- gitleaks directory scan: no leaks found

Raw aggregate exit code: 0

## Files touched

- justfile (doc comment for the security recipe; recipe body untouched)
- docs/architecture/README.md (renderer note under Regeneration)
- .report-12.md (this file)

No PR opened. Branch pushed for the orchestrator to open the PR.
